# 2者レビューの突合と修理計画（2026-08-05）

> 対象: `OWNER_ISSUE_MASTER_20260805.md`（オーナー指摘 241件）
> レビュー: `review_fable_20260805.md`（Fable＝このゲームを作った当人、241件判定）／`review_sol_20260805.md`（Sol/Codex＝作業を見ていない外部、239件判定）
> 裁定: 両者が割れた最重要点は CC が現行コードを読んで確定した（本文書 §2）

---

## 1. 判定はどれくらい割れたか

| | 件数 |
|---|---|
| 両者が判定した項目 | 239 |
| 一致 | 144 |
| **不一致** | **95** |
| └ Fable の方が甘い | **84（不一致の88%）** |
| └ Sol の方が甘い | 11 |

**不一致が一方向に偏っている。** ばらつきなら両方向に散るはずで、これは個別の判断ミスではなく系統的なズレ。**作った当人に自分の仕事を採点させると甘くなる**という形で出た。オーナーが「fableとsolそれぞれに」と2者を指定したことが、そのまま84件の差になっている。

| 判定 | Fable | Sol |
|---|---|---|
| 解決 | 149 | 137 |
| 部分解決 | 30 | **76** |
| 未解決 | 7 | **17** |
| 退行 | 0 | 2 |
| 判定不能 | 30 | 7 |
| 対象外 | 25 | 0 |

**どちらか一方でも「部分解決以下」とした項目＝残作業のあるもの: 97件。**

---

## 2. 割れた最重要点の裁定（CC が現物のコードで確定）

**論点**: プレー終了の12秒時間切れが、これまで積んだ歯止めを全部迂回するか。
Sol=「迂回する。8件が未解決」／Fable=「解決・部分解決」。

**裁定: Sol が正しい。** `baseball3d.html` の該当箇所の実行順は次のとおり。

```
① if(S.playClock > (inRundown?24:12)){ … return concludePlay(); }   ← ここで返る
② const flyWait   = フライ捕球直後の猶予
③ const keyHeld   = 走塁キーが押されているか
④ const allSettled = 全走者が塁に着いたか
⑤ if(T.target==='P' && !flyWait && !keyHeld && allSettled) return concludePlay();
```

①で `return` するため②〜⑤は評価すらされない。Fable が「解決」の根拠に挙げた行（`homing` による本塁継続）は `if(holder){ tagged }` 分岐の中にあり、時間切れの経路は通らない。

得点の確定も同様に `Math.round(r.goal)`＝**走者がどこへ向かっていたか**で数える。コード中のコメント自身が「打ち切りで塁間(例 p=3.7)にいた走者が得点だけ記録され、画面では三塁と本塁の間で止まったままになる」と書いており、**得点を止めずに見た目だけ合わせる**修正になっている。

**机上の話ではない**（本日の録画8プレーの実測）:

| 最長のプレー | 11.2秒（120407 play2） |
|---|---|
| 時間切れの閾値 | 12.0秒 |
| 余裕 | **0.8秒** |

---

## 3. 両者が独立に同じ結論へ達した機構（信頼度が高い）

Sol は6つ、Fable は3層で機構を挙げた。**うち4つが独立収束**している。

| # | 機構 | Sol | Fable | 該当 |
|---|---|---|---|---|
| A | **同じ量に複数の書き手がいて、後段が黙って上書きする** — 走者の行き先は打った瞬間（`2685-2695`）と捕った瞬間（`1766-1778`）の両方が書き、後者が前者を消す | ①（退行と明記） | ①（ライブ再現で確認） | OI-089/140/141/238 |
| B | **担当交代に慣性がない・外された野手が無役になる** — 着地後0.35秒ごとに主担当を再計算し、交代閾値も最低保持時間も旧担当の新目標も無い（`2753-2768`） | ③ | ①（3症状の統一と明記） | OI-190/232/237/240/241(b) |
| C | **回帰テストが指摘空間を覆わない** — sweep128 は1アウト固定・塁なし/一二塁・無入力の64打球×2のみ | ⑥ | ③ | OI-063 |
| D | **守備と走塁の内部状態が間接接続** — `1356` が内部goalからdirを作り、守備判断がdirを見る | ④ | 残存と明記 | OI-056 / Sol P5 |

**Fable だけが実証した重い事実**: 「本塁に帰っていないのに得点する」バグを**意図的にコードへ戻して**回帰テストを全実行したところ、**全PASSのまま**だった。オーナーが4回踏んだ得点バグについて、現行のテストは再発を検出できない。

**Sol だけが指摘した機構**: 上記②（12秒時間切れ・§2で裁定＝正しい）と⑤（牽制・盗塁が実距離の接触を確認せずアウトにする＝`2351-2385`）。

---

## 4. 修理の順番（どこを直すとどの指摘が一度に閉じるか）

| # | 直すもの | 閉じる指摘 | 根拠 |
|---|---|---|---|
| **1** | **プレー終了の時間切れと得点の確定** — 時間切れを歯止めの後ろに置き、得点は「向かっていた先」でなく実位置で数える | OI-060/073/080/137/144/186/213/230（Sol未解決8）＋OI-174/180/195/208（本塁未到達得点） = **12件** | §2でCC裁定済み。実測余裕0.8秒 |
| **2** | **走者の行き先の書き手を1つにする** — 捕球時の一般処理に「既に踏み切った走者は戻さない」適用条件を付ける | OI-089/140/141/238 = **4件**（うち2件は本日の再発） | A（独立収束・Fableがライブ再現） |
| **3** | **担当交代に現職慣性と役割を持たせる** — 交代閾値・最低保持時間・外された野手の新しい役割 | OI-190/232/237/240/241(b) = **5件**（うち3件は本日） | B（独立収束・録画2本で実測） |
| **4** | **回帰テストの守備範囲を広げる** — 0/2アウト・走塁キー操作・担当交代・得点時刻・牽制/盗塁を軸に加える。**わざと壊して落ちるか**で検出力を確かめる | OI-063（＝オーナーの「なぜ見つけられないのか」） | C（独立収束＋変異テスト実証） |
| **5** | **回り込みの適用条件** — チャージ判定を球速閾値（70ft/s）でなく「詰められるか」で決める | OI-241(a)/OI-127 の揺り戻し | 本日の録画で1.3秒・46ftを実測。Fable §8-2 に案 |
| 6 | 実タッチの確認（牽制・盗塁） | Sol P2/P3、OI-157 | Sol⑤ |
| 7 | 守備判断が走者の内部状態を見ない形にする | OI-056、Sol P5 | D |

**1〜4を先にやる。** 5以降は体感の改善で、1〜3を直さないと効果が見えない。

---

## 5. オーナーの判断が要るもの（CCが勝手に決めない）

| # | 論点 | 状況 |
|---|---|---|
| 1 | **テクスチャ「リアルと見間違えるクオリティ」（OI-217）** | **両者とも未達と判定**。単一HTML・外部画像なしという制約下ではランタイム生成が天井で、写真品質には届かない。制約を変える（画像ファイルを別に置く／3Dモデルを外部から読む）か、目標を下げるか。**この制約はこれまでオーナーに伝えていなかった** |
| 2 | **難易度を打撃・投球・守備で別々に選ぶ（OI-169）** | 両者一致で未実装。機能追加なので、やるかどうか |
| 3 | **ベンチ・観客席へ入った悪送球の2個進塁（OI-171）** | 両者一致で未実装。規則の実装 |
| 4 | **12秒の時間切れをどうするか** | 撤廃するとプレーが終わらなくなる場面が出うる。CCの案＝時間切れは残すが「走者が塁間／キー押下中／球が空中」の間は延長し、得点は実位置で数える。この方針でよいか |

---

## 6. この2つのレビューでも見つからないもの（両者の申告）

- 見た目・カメラ・モーション・テクスチャの自然さ（実際に描画して人が見ないと判定できない）
- 得点分布・盗塁成功率・打球種の分布・内野安打率（大量試行の統計テストが無い）
- 低頻度のランダム不具合（固定seedと十分な試行数が無いと「無い」ことを示せない）
- OI-237 の該当録画は失われている（Fキーは直前1プレーのみ）。次に見かけたら **Gキー**で保存が要る
