# Baseball3D 中核アーキテクチャ再設計計画

作成日: 2026-08-08
基準実装: BUILD `b0805-13` / `baseball3d.html` SHA-256 `16e00f796cd78a89a541df97c7b414feb683ae4fac318df76537eeaaa3acd683`
一次資料: OI-001〜253、録画50件、Claude Code元セッション4本、Git履歴20コミット、Sol Ultra工程0〜5

## 1. 最上位目的

最上位目的は、個別の録画をその場で直すことではなく、**同じ構造から別の症状が再生成されない状態にすること**である。

合格条件は次の順で評価する。

1. オーナーが報告した症状そのものを固定再現できる。
2. 修正前はFAIL、修正後はPASSになる。
3. 修正を意図的に戻すと専用テストがFAILする。
4. 隣接場面、統計水準、実ブラウザ表示に退行がない。
5. 最新BUILDで実際に1セッション遊び、新規指摘がない。

## 2. 監査で確定した構造的原因

### 2.1 同じ状態に複数の書き手がいる

`b0805-13`を基準にした静的監査では、少なくとも次の直接変更経路が残っていた。

| 状態 | 直接変更数 | 主な症状 |
|---|---:|---|
| 走者の`goal` | 19（コメント誤検出を除く） | 進塁後の逆走、個別選択漏れ、守備への意図漏洩 |
| 打球担当`primary` | 9 | 担当交代、旧担当の無役化、壁反射後の旧担当参照 |
| `coverBase` | 13 | カバー消失、別役割の奪取、無人塁送球 |
| `concludePlay()`呼出 | 10 | 塁間・ライブ球・入力中の打切り |

重要なのは件数そのものではない。**後段の書き手が、前段で正しく決めた状態を黙って上書きできること**が問題である。

### 2.2 同じ到達可能性を複数モデルが計算する

守備の「誰が、どこへ、いつ着けるか」は、`planPlay`、`interceptPoint`、`coverArrival`、再照準ループ等で別々に計算されている。1層だけ直しても別層が古い判断を実行するため、修理が体感へ反映されない。

### 2.3 場面パッチが共通経路へ漏れる

外野返球用の規則が内野ゴロへ適用されたOI-220、回り込み改善が平凡なゴロの遠回りへ反転したOI-241、捕球モーション追加が不要なダイブへ反転したOI-243が代表例である。

### 2.4 テストが症状空間を覆っていなかった

Sol Ultraの10変異監査では4変異を全PASSで見逃した。fixture腐敗も3件確認された。従って、テスト本数やPASS数ではなく、**対応変異を直接落とせるか**を採用基準にする。

### 2.5 正しいモデル修理が旧較正を無効化する

守備モデル修理前後でゴロ安打率が約24ポイント変化した例がある。構造修理後は必ず得点・出塁率・打球種・盗塁・送球等を再較正する。

## 3. 目標アーキテクチャ

### 3.1 RunnerIntent

唯一管理する状態:

- 自動進塁
- 手動S/X/Z/個別塁指定
- 帰塁義務
- タッチアップ
- 強制進塁
- 結果確定時の配置

原則:

- 他層から`r.goal=`を直接行わない。
- 変更元、更新番号、優先順位を記録する。
- 守備は`goal`・`cmd`を読まない。
- 守備が読めるのは実位置、実移動方向、実速度、フォース状態だけ。

### 3.2 FieldingAssignment

唯一管理する状態:

- 打球担当
- 各塁カバー
- 中継
- バックアップ

原則:

- 一人一役を基本とする。
- 役割変更は原子的に行い、旧役割を必ず再配置する。
- 担当交代は最低保持時間、改善幅、実移動コストを満たす時だけ許可する。

### 3.3 ReachModel

唯一計算するもの:

- 野手の反応＋加速＋走行時間
- 既に走行中の運動クレジット
- 方向転換コスト
- 捕球可能高さ・飛び込み到達距離
- カバー到着時刻

`planPlay`、`interceptPoint`、`coverArrival`はこの共通モデルを利用する。

### 3.4 ThrowDecision

入力:

- ボール保持者
- 走者の観測状態
- カバー到着予定
- アウト数・フォース状態
- 送球時間・中継時間

出力:

- 塁へ送球
- 中継
- 保持して詰める
- 回収
- 投手へ返球

原則:

- 副作用のない純粋関数に近づける。
- 攻撃側の`goal`、`cmd`、自動判断を入力にしない。
- リリース直前の再判断も同じ関数を通す。

### 3.5 PlayLifecycle

唯一管理するもの:

- アウト成立
- 得点時刻
- デッド/ライブ
- 続行可否
- プレー終了

終了条件:

- 3アウト後の得点・アピール処理完了
- 規則上のデッドボール処理完了
- または、全走者が塁上、入力なし、球静止/保球、未決着プレーなし

救済タイマーは上記判定の前に発火させない。

## 4. 移行方針

全面書き直しは行わない。単一HTML、描画、物理、録画機能を維持し、中核状態を段階的に移す。

### Phase 0: 固定・ラチェット

- 基準BUILD、commit SHA、HTML SHA、harness SHAを保存する。
- `_architecture_guard_20260808.js`で直接書き手数を上限固定する。
- 新規の直接書き手をCIで禁止する。
- 録画へ状態変更元と更新番号を追加する。

### Phase 1: RunnerIntent基盤 — BUILD b0805-14

このPRで実施する。

- `setRunnerIntent`、`setAutoGoal`、`setManualGoal`を導入。
- 手動操作経路から直接`goal`代入を除去。
- Z/1〜3キーの選択門番を打者走者の特別処理より前へ移動。
- 実移動方向`obsDir`を追加し、守備・塁判定から内部`goal`由来の`dir`参照を排除。
- 録画へ`intentSource / intentSeq / obsDir`を追加。
- OI-056、088、152、165の専用テストと変異テストを追加。

Phase 1完了後のラチェット:

- 直接`goal`書き込み: 19 → 15以下
- `applyRunnerKeys`の直接`goal`書き込み: 3 → 0
- `updateStealCommands`の直接`goal`書き込み: 1 → 0
- 守備判断関数の`goal/cmd/legacy dir`参照: 0

### Phase 2: RunnerIntent書き手の完全移行 — BUILD b0805-15

本PRで実施する。

- 捕球、挟殺、牽制、結果確定、ファウルの残る直接代入をAPIへ移す。
- `goal`と`autoGoal`の直接書き手を`setRunnerIntent`内の各1か所へ限定する。
- 自動・手動・規則・結果の優先順位と、決定元/更新番号の記録契約を固定する。
- architecture guardに意図的な第2書き手を注入し、CIが拒否することを確認する。

Phase 2完了後のラチェット:

- 直接`goal`書き込み: 15 → **1**
- 直接`autoGoal`書き込み: 4 → **1**
- 唯一の書き手: `setRunnerIntent`

残る課題:

- `goal`を数値だけでなく意図enumへ分離する作業は、50録画のコーパス化と合わせて後続RunnerIntent高度化で行う。

### Phase 3: FieldingAssignment基盤 — BUILD b0805-16

本PRで実施する。

- `primary`と`coverBase`の全変更を守備割当APIへ集約する。
- 役割変更元`roleSource`と更新番号`roleSeq`を録画へ追加する。
- `rundownCover`は既存同塁担当または無役野手だけを選び、別塁カバー/打球担当を奪わない。
- 中継・本塁カットでカバー野手を動かす場合は、代役を先に確保して原子的に再配置する。代役がいなければ引き抜かない。
- 壁反射で担当が変わった物理刻みは直ちに終了し、次刻みで新担当を再取得する。
- `resetFielders`はprimaryだけでなく全coverも消去する。

Phase 3完了後のラチェット:

- `primary`直接書き込み: 9 → **2**（両方`setPrimaryFielder`内）
- `coverBase`直接書き込み: 13 → **2**（clear/assign API内に各1）
- 役割奪取・元カバー消失・壁反射旧担当捕球の専用テストと変異検査をCIへ追加する。

### Phase 4: ReachModel基盤 — BUILD b0805-17

本PRで実施する。

- `reachTimeToPoint`を到達時刻の唯一の公開モデルとして追加する。
- 反応時間、加速、現在速度のクレジット、捕球半径、速度係数、方向転換コストを共通化する。
- `planPlay / interceptPoint / routeProb / coverArrival / 自分で踏む判断`を共通モデルへ移す。
- 動的再計画では、現在の走行方向から180度反転する場合に最大0.30秒の減速・再加速コストを課す。
- architecture guardで、`runTime`の直接利用をReachModel内部だけに限定する。

Phase 4基盤完了後のラチェット:

- production内の`runTime`利用: 7経路 → **1経路**（`reachTravelTime`内部）
- 到達判定を持つ主要4関数: 全て`reachTimeToPoint`を利用

後続:

- 追跡1000打球の余分距離分布をb0805-16と比較し、再照準の採用条件を統計的に詰める。

### Phase 5: ThrowDecision

- 送球先の初期判断とリリース直前判断を一本化。
- 観測状態だけを入力にする。
- 牽制・盗塁の実タッチ判定を分離する。

### Phase 6: PlayLifecycle

- 全`concludePlay()`入口をイベント要求へ置換。
- 球・走者・入力・アピールの共通不変条件を単一関数で判定。
- 未採番録画202809をOI-213の先行証拠として回帰化する。

### Phase 7: 全体再較正

- 最低50試合。
- 得点、OBP、ゴロ/ライナー/フライ安打率、HR、盗塁、悪送球、守備追跡を測る。
- 修理前後を同seedで比較する。

## 5. CIで必須にする検証

各PRは次を全て満たす。

1. JS構文検査
2. 正本ハーネス全PASS
3. architecture guard PASS
4. 対象症状の専用テストPASS
5. 対応変異が専用テストでFAIL
6. `git diff --check`
7. BUILD/commit/HTML SHA/harness SHAのログ保存

見た目を変更するPRは実ブラウザ画像確認を追加する。統計に影響するPRは試合水準検査を追加する。

## 6. 禁止事項

- 新しい場面パッチを共通関数へ適用条件なしで追加しない。
- production式をそのままテスト側へ複製しない。
- PASS件数だけで完成判定しない。
- fixture成立件数0をPASSにしない。
- 古いBUILDの録画を現行退行として扱わない。
- OI番号、再現録画、修正commit、専用テストの対応をUNKNOWNのまま「解決済み」にしない。

## 7. Phase 1の受入条件

- OI-088: Zで先頭走者だけを動かし、打者走者を変更しない。
- OI-152: 3+Sで三塁走者だけタッチアップ準備し、打者・一塁走者を変更しない。
- OI-165: 1+Sで一塁走者だけを変更し、打者・三塁走者を変更しない。
- OI-056: 同じ位置・速度・観測方向なら、内部goalだけ変えても守備の送球先が変わらない。
- goalを変えただけでは観測方向が反転せず、実際に逆方向へ動いたフレームでのみ反転する。
- 上記2系統の意図的変異を専用テストが検出する。


## Phase 4b — b0805-18 Retarget adoption gate

- 0.20秒ごとの再照準は無条件採用しない。
- 現在目標を続けるETAと候補目標ETAを共通ReachModelで比較する。
- 候補が0.12秒以上有利、または球が旧目標を通過済みの場合のみ切り替える。
- `aimSource / aimSeq` を録画へ追加し、目標変更の出所を追跡する。
- architecture guardで`stepFlight`の動的再照準が`retargetFielder`を経由することを固定する。


## Phase 5 — b0805-19 ThrowDecision

- 捕球時・リリース時・追加送球の自動送球先を`decideThrowTarget`へ集約する。
- `T.target`の後書きは`setThrowTarget`だけが行い、`decisionSource / decisionSeq`を録画へ残す。
- 手動指定、フライ、盗塁、牽制、中継の固定ルールを同じ優先順位表へ置く。
- 守備側の手動`P`指定が一塁へ変換される旧経路を廃止する。
- architecture guardで`chooseThrowTarget`の直接呼び出しと第二の`T.target`書き手を禁止する。


## Phase 6 — b0805-20 PlayLifecycle

- 全てのプレー終了要求を`requestPlayConclusion`へ集約する。
- `playLifecycleState`がライブ送球・塁間走者・有効な走塁入力・未決着挟殺・フライ判断猶予を一括判定する。
- 第三アウトのみ未解決条件を上書きして即終了可能。
- watchdogは`force=true`で門番を迂回せず、終了拒否時は同じフレームの回収・捕球・走塁処理を継続する。
- 旧`ball.land`参照を廃止し、送球stage＋球の高さ/速度からライブ状態を判断する。
- `lifecycleSource / lifecycleSeq / lifecycleReason`を録画へ残す。

- 追加検証で、終了gatewayを早期return位置へ機械置換すると拒否時にも呼出元がreturnし、ライブ送球が凍結することを検出。物理更新前の終了要求は「実際に閉じた時だけreturn」へ固定した。


## Phase 6b — b0805-21 Rundown exit intent

- 50試合固定seed検証で、挟殺中の仮`goal`（1.2/1.7等）が送球ミス後も残り、非アウト走者が塁間停止する4/50未終了を発見。
- g2固定seed `0xdfb07ee1` で `goal 1 → 1.9 → 1.2`、最後の更新が`R.fumble=true / loLim=1.2 / hiLim=1.8`であることを実行追跡。
- `settleRundownExitIntent`を追加し、挟殺終了時に仮の逃げ幅goalを対応する合法な塁`R.lo/R.hi`へRunnerIntent経由で清算する。
- PlayLifecycleの「塁間では閉じない」契約は緩めない。走者自身を合法な塁へ動かして決着させる。
