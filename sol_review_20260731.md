# baseball3d.html 残存欠陥監査

- 監査日: 2026-07-31
- 対象: `野球ゲーム/baseball3d.html`
- 読了範囲: 実ファイルは改行単位で2019行（依頼記載の2020行とは1行差）。全行を読んだ。
- 制約: この監査でCodexは`baseball3d.html`その他のプロジェクトファイルを変更していない。監査中に外部プロセスがHTMLを変更した事実は末尾に記録する。
- 判定方針: 実コードの行番号と逐語引用を根拠にし、実行条件が必要なものは条件を明記する。断定できないものは仮説として扱う。

## 1. 既存ハーネスが構造的に拾えない欠陥クラス

監査結果: 問題あり。既存の2592シナリオ／25試合ハーネスが確認した「終端・得点・アウト・runner conservation」は有用だが、次の入力・時間・描画経路を機械的には保証しない。

- 実時間差: 固定`dt`での実行は、画面更新率の違い、背景タブのタイマー制限、復帰時の`dt`上限を通らない。これは指摘4で扱った。
- 入力再入性: 結果演出中のキー、ポーズ中のマウス、送球前後の手動送球先変更を同じ試合状態へ差し込むテストが必要で、指摘1・2・13が該当する。
- RNG分岐: `Math.random()`はCPUのスイング、ファウル化、フェンス反射後の経路、持ち替え中の捕球ミス、送球受け取り時間に使われる（570-596行、623行、836-837行、897行、973行）。代表値1回の実行では低頻度経路を網羅しない。
- 描画の証拠: WebGL行列・深度・走者の実画面位置は、機械的な得点／アウト検査だけでは観測できない。指摘6・9・12はこの穴に入る。
- 仕様境界: 9回同点、満塁からの押し出し、複数アウト種別の成立順など、特定の初期状態と操作順を固定しないと出ないルール境界がある。指摘10・11が該当する。

未確定の低頻度仮説: `beginThrowPhase`の送球先選択は`A.transfer`だけで`T.transfer`に加わる乱数の捕球ミス遅延（836-837行）を見ていない。乱数が発火すると、事前に「間に合う」と選んだ送球先が実送球時には遅れる可能性がある。`Math.random`を固定して発火させ、同じ走者ETAで送球先選択と実到着を比較すれば確証できる。指摘8は肩力・距離の近似差を確定所見とし、この乱数部分は未確定として分離した。

## 2. 状態機械の空白・二重遷移

### 指摘1
行番号: 751-755, 831, 1859-1862, 1930-1935

現象: 守備側の「投手へ返球」操作（`0`キーまたは「投手」ボタン）が、投手ではなく一塁への送球指定に変換される。

再現条件: CPU攻撃中に打球が飛んでいる間、または送球開始前に`0`キーを押す／「投手」をクリックする。コードは次のとおり。

> `if(T.target!=='P' && Math.hypot(tp[0]-T.thrower.cx, tp[1]-T.thrower.cy) < 9){`
>
> `if(!isPlayerBatting() && manualThrow!=null) tgt = {nb: manualThrow==='P' ? 1 : manualThrow};`
>
> `if(throwPlay && throwPlay.stage==='transfer') throwPlay.target = (t==='P') ? 1 : t;`

なぜ問題か: `throwPoint('P')`には投手の現在位置を返す処理があるのに、手動入力経路だけが`'P'`を`1`へ変換している。そのため画面表示（「投手」）と実際の送球先（一塁）が一致せず、しかも一塁カバーがいない場面では、過去に修正された「カバーのない一塁へ投げる」失敗を手動操作で再発させる。なお、実行時の特定守備位置での見た目までは未実機確認だが、入力値の変換はコード上確定している。

修正案: `manualThrow==='P'`を`throwPlay.target`へそのまま渡し、`throwPoint('P')`と投手受け処理を通す。UIの表示値と内部の送球先コードを同じ値体系にする。

重大度: 高

### 指摘2
行番号: 1370-1379, 1472-1479, 1789-1802, 1842-1850

現象: プレー結果が確定して次の打者を準備している`phase='play'`中も、キーボードの走塁・送球操作だけが受け付けられる。

再現条件: 打球結果の大きな表示と走者の演出が続く約2〜6秒の間に、攻撃側なら`S`/`Z`/`X`、守備側なら`S`/`Z`/`C`/`X`/`0`を押す。コードはUIボタンを`flight`/`throwing`だけ有効にしている一方、キーボード側では`play`を`inPlay`に含めている。

> `S.phase='play'; S.timer=Math.min(6, Math.max(2.2, need, runNeed+0.8));`
>
> `const inPlay = (S.phase==='flight'||S.phase==='throwing'||S.phase==='play');`
>
> `if(inPlay && isPlayerBatting()){`
>
> `if(k==='s'){ advanceRunners(false); return; }`
>
> `if(k==='z'){ advanceRunners(true);  return; }`
>
> `if(k==='x'){ retreatRunners(); return; }`

なぜ問題か: `finishPlay`ですでに`S.bases`、得点、アウト、次打者が確定した後なのに、走者の`goal`だけを変更できる。表示上は記録と違う塁へ走り出し、最後に`r.p=r.goal`でさらに確定記録と違う位置へ瞬間移動してから、`afterPlay`で走者配列だけ消える。守備側の入力も確定後の`manualThrow`を変更できるため、状態機械上の「プレー中」と「結果演出中」が混ざっている。

修正案: キーボードの`inPlay`から`phase==='play'`を外す。結果演出を早送りするSpaceだけを別扱いにし、`phase='play'`では走者・送球の入力関数を一切呼ばない。

重大度: 高

### 指摘3
行番号: 539-553, 1838-1840, 1981-1989

現象: 試合終了画面をポーズした後に「もう1試合」を押すと、新しい試合もポーズ状態のまま始まる。

再現条件: 試合終了後に`P`でポーズし、その状態で表示された「もう1試合」を押す。`newGame()`はゲーム状態を初期化するが、`paused`と`rosterOpen`を初期化していない。選手能力画面については、再試合ボタンがモーダルの裏にあるため、この経路では直接押せず、主な再現経路は終了後のポーズである。

> `function newGame(){`
>
> `  S = {`
>
> `    phase:'msg', timer:1.6,`
>
> `    inning:1, half:0, outs:0, balls:0, strikes:0,`
>
> `}`
>
> `let paused=false, rosterOpen=false;`
>
> `const isPaused = ()=> paused || rosterOpen;`

なぜ問題か: 新しい`S.phase`は`msg`に戻るが、ループは`if(!isPaused()) update(dt);`で止まり続ける。表示上は新試合なのに時間が進まず、試合状態と実行条件が一致しない。

修正案: `newGame()`の開始時に`paused=false; rosterOpen=false;`を設定し、モーダルも閉じる。少なくとも再試合ボタンからの開始は必ず再開状態にする。

重大度: 中

状態遷移の確認済みで問題なし: `aimPitch`はCPU攻撃時に投球者がクリックするための意図的な待機状態で、`mousedown`から`launchPitch`へ抜けられる。`msg`、`windup`、`pitch`、`flight`、`throwing`、`play`、`over`の通常経路について、コード上で未使用の遷移先や通常操作だけで永遠に抜けられない段階は、上記の入力再入性を除いて確認できなかった。`throwPlay.stage`の`transfer`、`fly`、`catch`、`rundown`も各分岐に到達・終了処理がある。

## 3. 物理計算と判定・表示の不一致

### 指摘4
行番号: 646-653, 668-674, 1172-1177, 1993-2008

現象: 野手の先回り計算は常に1/240秒刻みなのに、実際の打球飛行は1フレームを4分割するだけで、画面更新間隔によって物理刻みが変わる。

再現条件: 60fps以外の画面更新、低fps、または背景タブ復帰後のタイマー駆動で打球を処理する。同じ打球でも、例えば30fpsなら実飛行の1ステップは約1/120秒、60fpsなら約1/240秒になる。

> `const b={...src}; let t=0; const path=[]; const h=1/240;`
>
> `for(let i=0;i<1700;i++){`
>
> `  stepBall(b,h); t+=h;`
>
> `const sub=4,h=dt/sub;`
>
> `for(let i=0;i<sub;i++){ stepBall(ball,h); ball.t+=h; }`
>
> `const dt=Math.min(0.05,(now-last)/1000); last=now;`

なぜ問題か: `stepBall`には速度依存の空気抵抗、重力、着地時の跳ね返り・減速があるため、刻み幅を変えると着地点・最高点・壁到達時刻が変わる。`planPlay`が選んだ primary、捕球候補、外野/内野判定は1/240秒の先読みなのに、`updateFlight`が進める実球は画面更新間隔依存で、野手・ボール・判定が同じ物理を見ていない。背景タブ用の`setInterval`も`dt`を最大0.05秒に切るため、ブラウザがタイマーを1秒間隔へ制限すると実時間に対してゲーム進行が約20分の1になり、復帰までプレーが異常に長くなる。60fps固定のハーネスではこの差を構造的に再現できない。

修正案: 物理を固定刻み（例: 1/240秒）で蓄積時間分だけ回し、描画フレームとは切り離す。先読みも実飛行も同じ固定刻みを使う。背景復帰時は経過時間を安全な上限付き複数ステップで消化するか、ゲーム内時間を止めるなら表示と入力を明示的にポーズ状態へ戻す。

重大度: 高

### 指摘5
行番号: 1369-1379, 1472-1478

現象: 走者が実際に目標塁へ着くまでの時間を計算しているのに、結果演出の待ち時間を最大6秒に切り、残り0.45秒で走者を目標位置へ瞬間移動させる。

再現条件: 本塁打、深い長打、または走力の低い走者が複数いるプレーで、打球が確定した時点の走者から生還・進塁先までの残り時間が5.2秒を超える。

> `const runNeed = runners.reduce((m,r)=>{`
>
> `  if(r.out) return m;`
>
> `  return Math.max(m, r.p<r.goal ? runnerETA(r,r.goal) : Math.abs(r.goal-r.p)/(RUN_SPEED*(r.sp||23)));`
>
> `S.phase='play'; S.timer=Math.min(6, Math.max(2.2, need, runNeed+0.8));`
>
> `if(S.timer<0.45) runners.forEach(r=>{ if(!r.out) r.p=r.goal; });`

なぜ問題か: `runNeed`を求める目的は「走者が走り終わるまで画面を保持する」ことなのに、`Math.min(6,...)`でその保証を破っている。実際の走者はまだ塁間にいるのに、最後の0.45秒で表示だけが目的塁へ移り、記録済みの`S.bases`と見た目が急に一致する。得点・アウトの統計はすでに確定しているため、後から走者が追いつくのではなく、演出だけが飛ぶ。

修正案: 物理演出を完了させるまで上限で切らない。安全上の上限が必要なら、走者を瞬間移動させずに演出を明示的に短縮するか、結果確定後の表示を記録済み塁表示へ切り替える。少なくとも`runNeed`を上回る固定上限と終端テレポートを同時に使わない。

重大度: 高

### 指摘6
行番号: 668-674, 727-735, 1180-1185, 1221-1223

現象: フェンスに当たってからの再計画だけ、野手の現在位置ではなく定位置からの距離で到達可能性を計算する。

再現条件: 打球が一度壁に当たり、壁に当たるまでに野手が打球方向へ移動しているプレー。壁反射後に`planPlay(ball)`が再び呼ばれる。

> `if(F_REACT+runTime(f.sp,Math.hypot(p.x-f.x,p.y-f.y)) <= p.t){ hit={t:p.t,p}; break; }`
>
> `hit={t:F_REACT+runTime(f.sp,Math.hypot(p.x-f.x,p.y-f.y)), p};`
>
> `const dx=f.tx-f.cx, dy=f.ty-f.cy, d=Math.hypot(dx,dy);`
>
> `ball.primary=plan.f; ball.planT=ball.t+plan.t;`

なぜ問題か: 実際の移動は`f.cx/f.cy`から始まるのに、先読みは`f.x/f.y`（定位置）から始まる。壁反射後はその差が数十フィートにもなり得るため、既に近くまで来ている野手を遅いと判定したり、逆に遠い野手を捕球担当にしたりする。再計画直後の同じフレームでは`const prim=ball.primary`も旧担当を保持しているため、新担当を設定した後の捕球・着地分岐が旧担当を参照する時間差もある。具体的な勝敗差は打球位置とフレームに依存するため実機再生は未検証だが、座標参照の不一致は確定している。

修正案: `planPlay`に基準位置を渡し、距離を`f.cx/f.cy`から計算する。壁反射後はローカルの`prim`も再取得してから同フレームの捕球・着地判定を行う。

重大度: 中

### 指摘7
行番号: 696-709, 771-776, 1374-1378

現象: 帰塁する走者の待ち時間だけ、実際の更新速度より速い速度で計算される。

再現条件: フライ捕球後に戻る走者、または打球中に`X`で帰塁を指示した走者が、結果確定時点で目標塁より先にいる。

> `}else if(r.p > r.goal){`
>
> `  r.v=Math.min(top*0.8,(r.v||0)+acc*dt);`
>
> `  r.p=Math.max(r.goal, r.p - r.v*dt*RUN_SPEED);`
>
> `return Math.max(m, r.p<r.goal ? runnerETA(r,r.goal) : Math.abs(r.goal-r.p)/(RUN_SPEED*(r.sp||23)));`

なぜ問題か: `updateRunners`は帰塁時の上限を`top*0.8`にしているが、`finishPlay`の帰塁側ETAは`top`相当の`RUN_SPEED*r.sp`で割っている。加速も考慮していないため、待ち時間が実走より短くなり、終端の`r.p=r.goal`で帰塁を瞬間移動する。前進側の`runnerETA`は加速を考慮しているのに、帰塁だけ別式になっている。

修正案: 前進・帰塁で同じ速度上限と加速モデルを使う逆向きETA関数を作る。`runNeed`の上限を終端テレポートで補わず、計算式と`updateRunners`を一つの関数から共有する。

重大度: 中

### 指摘8
行番号: 813-825, 757-768, 889-898

現象: 守備側がどの塁へ送球するかの事前判定が、実際の肩力別の送球物理を使わず、全選手共通の`距離/105 + 0.18`で判断する。

再現条件: 送球先候補が複数あり、走者の到達時刻と送球到達時刻が僅差のプレー。特に肩の弱い野手、遠い塁、捕球ミスによる持ち替え遅延がある送球で起きる。

> `const eta = transferGuess + Math.hypot(p[0]-f.cx,p[1]-f.cy)/105 + 0.18;`
>
> `const v0=(58+36*arm)*1.02;`
>
> `const s=Math.min(0.95, 32.2*d/(v0*v0));`
>
> `T.catchDur = rnd(0.04,0.30)*(1.30-T.thrower.arm)*2.0;`

なぜ問題か: 送球先選択では肩力を使わない近似速度105ft/sと固定0.18秒を使う一方、実送球は`arm`で初速が変わり、距離に応じた投射角と受け取り時間も加わる。結果として「刺せる見込みあり」と選んだ塁で実際にはセーフ、または安全な別塁を選ばない、という計画と判定のずれが境界ケースで起きる。全量シナリオの勝敗統計だけでは平均に埋もれる種類の欠陥である。

修正案: 送球開始前の候補比較を、実際の`launchThrow`と同じ初速・投射・受け取り時間・中継経路から算出する。近似を残すなら、肩力と受け手の捕球条件を同じ式で使い、境界に安全余裕を持たせる。

重大度: 中

### 指摘9
行番号: 867-885, 727-735, 751-755

現象: 投手への返球は、投手の現在位置を送球先として記録するが、送球中に投手をその場所へ止めない。

再現条件: フライ捕球後に返球先が`P`になる、または守備側が`0`を指定した送球経路（指摘1の変換不具合を修正した後を含む）。投手が打球方向へ走っている最中に送球が開始される。

> `let p = (T.target==='P' && recv) ? [recv.cx, recv.cy] : throwPoint(T.target);`
>
> `launchThrow(T.thrower.cx,T.thrower.cy,p[0],p[1],T.thrower.arm);`
>
> `if(recv && recv.n!=='投') setTarget(recv,p[0],p[1]);   // 受け手はその場で待つ`

なぜ問題か: 投手以外は`setTarget`で送球先に待機させるのに、投手だけは除外される。送球は開始時の座標へ飛ぶが、投手はその後も移動し、到着判定は古い`T.dest`で行われ、その直後にボールを移動後の投手の手へ置く。画面ではボールが投手の現在位置へ飛ばず、到着時に跳ぶ可能性がある。これは送球物理・受け手位置・表示の不一致である。

修正案: 投手を受け手にした場合も投手を送球先で停止させる、または受け手の移動を含む追跡送球として到達判定を実装する。固定点へ投げるなら、`T.dest`到着まで受け手を動かさない。

重大度: 中

物理・判定の確認済みで問題なし: 捕球可能高さの閾値は、先読み側の`p.z <= (p.vz<0 ? 9.5 : 7.0)`（662行）と実判定側の`const reach = (ball.vz<0) ? 9.5 : 7.0`（1234-1236行）で一致している。初期位置・通常の60fps相当の刻みでは、走者速度と守備速度も同じ`A.speed`由来の値を使っており、ここ単独の閾値不一致は確認できなかった。

## 4. 野球ルールの判定

### 指摘10
行番号: 1058-1080

現象: 得点を無効にする条件が「第三アウトがforce/flyだったか」ではなく、「そのプレー中にforce/flyアウトが1つでもあったか」になっている。

再現条件: 1プレー中に、先に`outKind='force'`または`'fly'`のアウトが記録され、その後に`outKind='tag'`の第三アウトが発生し、先に走者が生還している複合プレー。通常の機械的な単一アウト／通常併殺だけでは出にくい境界ケースである。

> `const outs=countOuts();`
>
> `if(S.outs+outs>=3 && runners.some(r=>r.out && (r.outKind==='force'||r.outKind==='fly'))) runs=0;`

なぜ問題か: ルール上、第三アウトが封殺・打者走者の一塁到達前アウト・フライ捕球なら得点を消すが、第三アウトがタッチアウトなら、先に成立した得点は認められる場合がある。上のコードは「どのアウトが第三アウトか」を保持せず、同じプレーの前半にforce/flyがあっただけで全得点を0にする。したがって、混合アウトの順序で表示得点が誤る。依頼で示されたハーネスの「第三アウトの得点」結果と直接矛盾するケースは、ハーネスがこの混合順序を生成していない限り観測されない。コード上の条件欠落は確定している。

修正案: アウトを成立順の配列で記録し、`outs`の最後の1件が得点無効種別かを判定する。第三アウトが`tag`なら、先に生還した走者の得点を残す。アウトの成立順が記録できないプレーは、判定前に明示的にその順序を確定する。

重大度: 高

### 指摘11
行番号: 55, 1399-1413

現象: 画面上は「9イニング」と表示しているが、9回裏同点では試合を終えず、最大12回まで進み、12回終了時に同点でも引き分けで終了する。

再現条件: 9回終了時点で両チーム同点にし、10〜12回も同点のまま進める。

> `<h1>BASEBALL 3D — 投打守フル / 9イニング</h1>`
>
> `if(S.inning>=9 && total(0)!==total(1)) return gameOver();`
>
> `if(S.inning>=12) return gameOver();`

なぜ問題か: 9回裏終了時に同点なら最初の条件が偽になり、10回へ進む。12回裏終了まで同点でも`gameOver()`へ進むため、タイトルの9イニング仕様と実際の試合終了条件が一致しない。既存の25ゲームで引き分けが無かったことは、同点を意図的に維持した試合を含まなかっただけで、この分岐を検証していない。

修正案: 9回制を正本とするなら、9回裏終了時は勝敗にかかわらず`gameOver()`へ進める。同点を延長で処理する仕様なら、タイトル・表示・テスト条件を「最大12回」に明示し、12回同点の扱いを仕様化する。

重大度: 中

ルール判定の確認済みで問題なし: 内野の封殺と非強制走者のタッチ／挟殺は、`judgeAtBase`の`isForced(cand)`分岐（1031-1041行）と`beginRundown`分岐で分けている。フライ捕球後は、離塁走者を元の塁へ戻し、タッチアップ指示または深い打球の三塁走者を進める処理がある（1267-1296行）。満塁四球は`forcedWalk`で三塁走者を得点させ、他走者を一つずつ押し出している（1435-1458行）。グラウンドルール・ダブルは柵を越えたが高さ9.4ft以下の打球を`entitled`で二塁打として処理している（1203-1211行、1323-1328行）。これらについて、読んだコード上で別の確定欠陥は見つからなかった。

## 5. 描画・入力・ポーズ

### 指摘12
行番号: 1504-1506, 1346-1351, 682-685, 1567-1583

現象: ファウルになった打球で走者が打球中の位置に残り、次の投球が始まるまで記録上の塁と画面上の走者位置がずれる。

再現条件: 走者がいる状態で、打球が着地する前にファウルになる。`doSwing`から`foulBall()`へ入り、または飛行中にファウル線を越えて`foulBall()`が呼ばれるケース。打球中は`updateRunners`が走者を動かしている。

> `if(Math.abs(c.spray)>45 || (c.q<0.40 && Math.random()<0.55)) return foulBall();`
>
> `function foulBall(){`
>
> `  if(S.strikes<2) S.strikes++;`
>
> `  ball=null; resetFielders(); S.fieldView=false;`
>
> `setMsg('ファウル','','#95a3b4'); S.lastPlay='ファウル';`
> `S.phase='msg'; S.timer=0.9; snapCam(); ui();`
>
> `runners = [{p:0, goal:1, autoGoal:1, extra:0, origin:0, sp:A.speed(bat.走)}];`
> `bases.forEach((b,i)=>{ if(b) runners.push({p:i+1.12, goal:i+1.45, autoGoal:i+1.45, extra:0, origin:i+1, sp:(b.sp||21)}); });`
> `if(runners.length){`
> `runners.forEach(r=>{`
> `const q=runnerPos(r);`

なぜ問題か: ファウル処理はボールと野手をリセットするが、走者の`p`と`goal`を元の塁へ戻さず、`runners`配列も消さない。次の投球で`startFlight`が呼ばれるまで`beginRunners(S.bases)`は実行されないため、`draw`は`S.bases`より優先して動いた走者を描く。ファウルは得点・アウトを確定させないのに、走者だけが塁間にいる表示になる。

修正案: `foulBall()`で全走者の`goal`と`p`を記録済みの`S.bases`へ戻すか、走者配列を消して描画を`S.bases`へ戻す。リードを演出する設計なら、ファウル時だけ安全に元の塁へ戻るアニメーションを完了させてから次の投球へ進める。

重大度: 中

### 指摘13
行番号: 1769-1778, 1780-1785

現象: ポーズ中でもマウス移動だけは投球の狙い`aim`を書き換える。

再現条件: `P`でポーズ、または選手能力画面を開いた後、キャンバス上でマウスを動かしてから再開する。

> `stage.addEventListener('mousemove', e=>{ const p=screenToZone(e); if(p){ aim.x=p.x; aim.y=p.y; } });`
>
> `stage.addEventListener('mousedown', e=>{`
>
> `  if(S.over || isPaused()) return;`
>
> `window.addEventListener('keydown', e=>{`
> `if(isPaused()) return;                       // ポーズ中は操作を受け付けない`

なぜ問題か: クリックとキーボードにはポーズ抑制があるのに、`mousemove`にはない。ポーズ画面の裏で狙いだけが変わり、再開時にプレーヤーが意図していない投球位置へ変わる。時間は進まないため直接の得点不整合ではないが、「ポーズ中は入力を受け付けない」という表示・入力契約に反する。

修正案: `mousemove`の先頭で`if(S.over || isPaused()) return;`を入れる。ポーズ中もカーソル表示だけを更新したい場合は、ゲームの`aim`とは別の表示用座標を持つ。

重大度: 低

描画の確認済みで問題なし: 行列の積は`PROJ * VIEW`として使われ、スクリーン座標の逆変換も同じ`VP`を反転している（1542-1547行、1754-1767行）。深度テストは通常描画で有効、半透明の影・トレイル・ストライクゾーンでは深度書込みを止めて描画後に戻している（1537-1547行、1625-1640行）。裏面カリングも明示的に無効化されている（188-190行）。読んだ範囲では、行列・カリング・深度設定による確定した消失・前後関係の欠陥は見つからなかった。

## 6. 数値の妥当性・能力値変換

### 指摘14
行番号: 447-460, 493-499, 646-674

現象: 守備力から変換した「持ち場から離れる不利さ」`A.zone`が、野手の守備範囲・捕球判定に一度も使われていない。

再現条件: 守備位置と走力・捕球・肩力が同じで、守備力だけが異なる野手を比較する。`buildFielders`では値を保存するが、`planPlay`の到達判定は`f.sp`、反応時間、距離だけで行う。

> `zone:  v => 0.0175 - 0.00016*v,         // 持ち場から離れる不利さ 0.0175〜0.0015`
>
> `return {n:p.pos, name:p.n, x:h.x, y:h.y,`
>
> `  sp:A.speed(p.走), zone:A.zone(p.守), arm:A.arm(p.肩), fld:p.守, cat:p.捕,`
> `  cx:h.x, cy:h.y, tx:h.x, ty:h.y, primary:false, run:0, face:0, v:0};`
>
> `if(F_REACT+runTime(f.sp,Math.hypot(p.x-f.x,p.y-f.y)) <= p.t){ hit={t:p.t,p}; break; }`

なぜ問題か: コメントと能力表示は守備力が「守備範囲の広さ」に効くと説明するが、実際の先読みは走力`sp`と固定反応時間だけで、`f.zone`を参照しない。守備力は送球の持ち替え時間など別経路には効くが、範囲の広さという変換軸は死んでいるため、守備力を変えても打球への到達範囲が変わらない。

修正案: 野手の到達可能距離または反応開始・到達時刻へ`f.zone`を一貫して組み込み、`planPlay`と実際の追跡・捕球判定で同じ式を使う。能力説明から削る場合は、表示と設計書も「守備範囲」に言及しないよう揃える。

重大度: 中

### 指摘15
行番号: 451-458, 757-766

現象: 肩力の説明に書かれた送球速度と、実際の変換式から出る速度が一致しない。

再現条件: 肩力1または100の境界値を能力値として計算する。実コードは`A.arm`を係数にしてから`launchThrow`で初速を作る。

> `arm:   v => 0.55 + 0.0050*v,            // 送球初速の係数 0.55〜1.05`
>
> `肩力1で約45mph、肩力100で約72mph。`
>
> `const v0=(58+36*arm)*1.02;`

なぜ問題か: 上の式をft/sからmphへ換算すると、肩力1では約54mph、肩力100では約67mphになる（1ft/s≒0.682mph）。説明値より境界が約9mph／約5mphずれており、能力画面・設計判断・速度調整の基準が一致していない。ゲーム内バランスが意図的にこの式なら説明が誤り、説明を正とするなら式が誤りである。

修正案: 実測したい速度レンジを先に固定し、`A.arm`または`v0`の係数を再計算する。少なくともコメントと能力説明を現在の式から得られる値へ更新する。

重大度: 低

数値変換の確認済みで問題なし: `runTime`の加速区間と定速区間、`A.transfer`の守備力・捕球の合成、`A.fumble`の捕球値依存、`A.power`・`A.loft`の打球初速／弾道変換は、定義と利用箇所が対応している（455-469行、616-633行、816-837行）。ただし、現実のプロ野球値との校正データはこのHTML内にないため、速度・捕球半径・反応時間が実際に妥当かまではコードだけでは検証不能であり、そこは未検証とする。


## 7. 既存Claudeテストハーネスの評価

### 構造的に検出できない穴

1. 固定`dt`での機械実行は、`updateFlight`の画面更新率依存刻み（1172-1177行）、背景タブの`setInterval`フォールバック（1991-2008行）、フレーム間の入力差を検査しない。
2. 2592通りのバッティング結果と4通りの走塁入力でも、結果確定後の`phase='play'`中にキーを押す再入性（1789-1802行）、ポーズ中のマウス移動（1769行）、投手返球コード`P`の変換（831行、1934行）を含めなければ検出しない。
3. `Math.random()`のCPUスイング、ファウル化、捕球ミス遅延、送球受け取り時間（570-596行、623行、836-837行、897行、973行）は、代表乱数や少数のフルゲームでは低頻度経路を保証しない。
4. 得点・アウト・runner conservationの検査は、WebGL上の走者位置、ボールの瞬間移動、壁反射後の野手担当、行列・深度表示を検出しない。
5. 9回同点を12回まで延長する分岐（1408-1410行）や、同一プレー内のforce/flyアウトとtagアウトの成立順（1075-1079行）は、初期状態と操作順を固定しないと検査されない。

### 既存結果との矛盾

- 直接の矛盾はない。報告された2592シナリオの「全プレー終端、アウト上限、得点範囲、runner conservation、表示塁」、25試合の「9回未満終了なし・得点整合・守備交代・停止なし」、平均得点10.8は、今回の指摘と両立する。これらは主に確定後の記録と終端を見ており、途中表示・入力再入性・実時間差・能力値の未使用を保証しない。
- 指摘10だけは条件付きで、もし既存ハーネスが「同一プレー中に先行force/flyアウト、最後にtagアウト、先行得点あり」の成立順まで生成していたなら、報告された「第三アウトの得点」結果と矛盾する。その具体的な混合順序がハーネスに含まれる証拠は今回の依頼文にはなく、含まれない前提で穴として扱った。
- 指摘11は、25試合で同点が発生しなかったことと矛盾しない。9回同点を意図的に固定した試合を追加しないと、12回終了の引き分け分岐は通らない。

### 監査の補助確認

HTML内JavaScriptの構文検証は、対象HTMLが監査後に外部変更されたため、この監査では指定SHA版に対する新規実行結果としては扱わない。構文が通ることだけではゲームルールや描画の正しさも証明できない。






## 追加所見（既存レポートを保持した追補）

### 指摘16

- 行番号: 1226-1236
- 現象: 実際の捕球半径はコメント上の4.5ftではなく、野手に近づいているフレームでは1.2ft未満でなければ捕球判定に入らない。
- 再現条件: フライが野手へ近づき、サンプリングされた距離が例えば `d2=2.0` → `1.5` → `5.0` ft のように、最接近が1.2ft以上かつ4.5ft未満なのに次のサンプルで4.5ftを越える場合。各値はコードのサンプリング結果としての例で、実際の値は打球速度・フレーム間隔に依存する。
- なぜ問題か: コードは捕球範囲を4.5ftと説明しているが、接近中は `closing` が真なので `d2<1.2` が追加条件になる。最接近後の次フレームで4.5ft以内なら捕れる場合もあるものの、フレームをまたいで4.5ftを越えると、野手がグラブ範囲に入った打球を安打として扱う。表示された「届く距離」と判定条件が一致しない。
  > `const closing = d2 <= (ball.dPrev===undefined ? 1e9 : ball.dPrev);`
  > `const reach = (ball.vz<0) ? 9.5 : 7.0;`
  > `if(!ball.landed && ball.canCatchAir && ball.t>0.45 && ball.z>1.0 && ball.z<reach && d2<4.5 && (!closing || d2<1.2)){`
- 修正案: 捕球半径を一つの定数にし、最接近判定をするなら「前フレームまでの最小距離」を保存して、4.5ft以内に入った時点または区間内に入った時点を一貫して捕球とする。`1.2`ftを残すなら、コメントと`planPlay`側の捕球可能範囲も同じ値にする。
- 重大度: 高

### 指摘17

- 行番号: 1194-1200, 1238-1244, 1361-1378, 1472-1478
- 現象: 2アウトでフライを捕球したとき、記録上は走者を元の塁に残すのに、捕球前に `S` / `Z` で走らせた走者の表示目標は戻されない。
- 再現条件: 攻撃側が2アウト、走者ありの状態で高いフライを打ち、捕球までに `S` または `Z` を押す。`advanceRunners`が走者の`goal`を次の塁へ設定したまま、捕球に入る。
- なぜ問題か: 3アウト目の特別分岐は`bases:nb`だけを元の塁で作り、`moves:null`のまま`finishPlay`へ渡す。`finishPlay`は`res.moves`があるときだけ走者目標を更新するため、描画用`runners`は進塁目標を保持し、HUDの記録済み塁と別の場所へ走る。最後には`r.p=r.goal`でその誤った目標へ寄せてから、`afterPlay`で配列だけを消す。
  > `if(!ball.landed && !ball.retreated && ball.canCatchAir && ball.t>0.35 && ball.maxZ>15){`
  > `runners.forEach(r=>{ if(r.origin>0 && r.cmd!=='S') setAutoGoal(r, r.origin); });`
  > `if(S.outs+1>=3){`
  > `return finishPlay({out:1, runs:0, bases:nb, hit:false, moves:null, throwTo:null,`
  > `if(res.moves) applyRunnerResult(res);`
  > `if(S.timer<0.45) runners.forEach(r=>{ if(!r.out) r.p=r.goal; });`
- 修正案: 3アウト特別分岐で全走者の`goal`を`origin`へ戻し、`cmd`も消す。あるいは`moves`を元の塁への移動として渡し、記録用`S.bases`と描画用走者を同じ確定結果から更新する。
- 重大度: 高

### 指摘18

- 行番号: 928-935, 944-950, 965-993
- 現象: ランダウン開始時に、実際に送球を受けた野手ではなく、塁に最も近い別の野手をボール保持者として再選択する。また、切り返し先の受け手をその塁へ移動させる指定がない。
- 再現条件: 外野安打で安全な到達塁より先を狙い、送球を受けた野手からランダウンに入る。`coverOf(hi)`が直前の`T.receiver`と異なる野手を返す、または`coverOf(chasing, R.holder)`が現在の目標塁から離れた野手を返す配置で起きる。
- なぜ問題か: `beginRundown`は`T.receiver`を使わず`holder:coverOf(hi)`を設定するため、次の更新でボールが別野手の手元へ瞬間移動する。切り返しでは`launchThrow`の到着先に対して`R.recv`へ`setTarget`していないので、ボールが到着した時点で受け手が塁にいなくても`R.holder=R.recv`となり、ボールだけが受け手へ移る。ランダウンのボール位置・野手位置・タッチ判定が一致しない。
  > `T.rd={ r, hi, lo, loLim:lo, hiLim:hi, holder:coverOf(hi), sub:'chase', t:0, ex:0 };`
  > `ball.x=R.holder.cx; ball.y=R.holder.cy; ball.z=4.4;`
  > `const recv=coverOf(chasing, R.holder);`
  > `launchThrow(R.holder.cx,R.holder.cy,p[0],p[1],R.holder.arm);`
  > `R.sub='relay'; R.recv=recv; R.dest=p; R.rt=0;`
  > `R.holder=R.recv; R.sub='chase';`
- 修正案: ランダウン開始時の保持者を`T.receiver||T.thrower`に固定し、切り返し送球では`setTarget(R.recv,p[0],p[1])`を必ず行う。受け手が到着していない場合は、到着までボールを捕球扱いにしない。
- 重大度: 高

### 指摘19

- 行番号: 827-829, 1007-1019, 1277-1289
- 現象: 複数の走者がフライ捕球前に離塁した場合、送球先の塁と無関係な「最も離れた走者」を離塁アウトにできる。
- 再現条件: 走者を2人以上置き、フライ中に`Z`などで複数走者を進ませ、捕球後にランダウンになる。`beginThrowPhase`が選んだ`flyLead`の塁と、別の走者の離塁距離の大小が異なる場合に起きる。
- なぜ問題か: フライ後の送球先は`flyLead.origin`から決められるが、`judgeAtBase(b)`の`back`検索は`b`または対象走者の塁を条件にせず、全走者から離塁距離最大の1人を選ぶ。例えば一塁へ投げたのに三塁走者をアウト扱いにするなど、送球の物理位置とアウトの対象が一致しない。
  > `if(kind==='fly') tgt = flyLead ? {nb: flyLead.mustReturn ? flyLead.origin : Math.min(4,flyLead.origin+1)} : {nb:'P'};`
  > `const back = runners.filter(r=>!r.out && r.mustReturn && Math.abs(r.p-r.origin)>0.02)`
  > `  .sort((x,y)=>Math.abs(y.p-y.origin)-Math.abs(x.p-x.origin))[0];`
  > `if(back && Math.abs(back.p-back.origin)>0.02){`
  > `  back.out=true; back.outKind='force'; back.goal=back.p;`
- 修正案: フライ後の送球対象を`flyLead`と対応付け、`judgeAtBase`では送球先の塁へ戻る対象だけを候補にする。複数走者の離塁は、1送球で誰をアウトにできるかを個別に記録し、他の走者を自動で同じ送球の対象にしない。
- 重大度: 高

### 指摘20

- 行番号: 1172-1179, 1201-1211, 1226-1237, 1993-1995
- 現象: 打球の物理は1フレーム内で4回進めるが、捕球・ファウル・フェンスのイベント判定はフレーム末尾の1点だけで行うため、フレーム更新率でアウト／安打／本塁打が変わり得る。
- 再現条件: 低い更新率または背景タブ復帰後に、打球が1フレーム中に野手の捕球半径、ファウル線、フェンスを横切る。`dt`は最大0.05秒なので、速い打球なら1フレームで数ft移動する。
- なぜ問題か: `stepBall`はサブステップ内で進むが、コードはその後にまずファウル・フェンスを判定し、さらに最後の`ball.x/ball.y/ball.z`だけで捕球を判定する。途中のサブステップで野手を通過していても、末尾点が範囲外なら捕球されない。逆に途中でフェンスを越えて戻ったイベント順序も、末尾のフェンス判定が先に処理する。固定dtの機械試験と実ブラウザのフレーム更新で結果が変わる。
  > `for(let i=0;i<sub;i++){`
  > `  stepBall(ball,h); ball.t+=h;`
  > `}`
  > `if(Math.abs(ang)>45.5 && (ball.landed||dist>90)) return foulBall();`
  > `if(dist>fd && Math.abs(ang)<=45.5){`
  > `if(!ball.landed && ball.canCatchAir && ball.t>0.45 && ball.z>1.0 && ball.z<reach && d2<4.5 && (!closing || d2<1.2)){`
- 修正案: 物理サブステップごとにイベントを判定し、区間交差（捕球球体・ファウル線・フェンス面）を使って最初に起きたイベントだけを採用する。少なくとも捕球判定用の前回位置・今回位置の線分交差を実装し、描画フレームの末尾点だけに依存しない。
- 重大度: 高

### 指摘21

- 行番号: 1789-1802, 1951-1966, 704-705
- 現象: `S`または`Z`を押し続けると、ブラウザのキーリピートで「1回押したら1つだけ」の進塁指示が複数回発火し、走者を複数塁進められる。
- 再現条件: 打球中に攻撃側で`S`または`Z`を押しっぱなしにする。`keydown`側に`e.repeat`の抑制がなく、走者が一塁へ着いて`cmd`を消費した後も、同じキーの自動リピートが`advanceRunners`を再度呼ぶ。
- なぜ問題か: コメントとUIは1回の指示を1塁分としているのに、入力イベントの回数がそのまま進塁回数になる。意図しない二塁・三塁への強行、送球開始前の目標変更、結果の違うランダウンが発生する。単発キーを1回だけ送る既存ハーネスでは検出できない。
  > `const inPlay = (S.phase==='flight'||S.phase==='throwing'||S.phase==='play');`
  > `if(k==='s'){ advanceRunners(false); return; }`
  > `if(k==='z'){ advanceRunners(true);  return; }`
  > `/* 進塁。1回押したら1つだけ進む「一発の指示」で、塁に着いたら指示は消える`
  > `if(r.p>=r.goal-1e-9 && r.cmd==='S') r.cmd=null;`
- 修正案: `keydown`で`e.repeat`を無視する、または押下中フラグを`keyup`で解除して1押下1命令にする。ボタン操作とキーボード操作を同じ一発指示経路へ統一する。
- 重大度: 中

### 指摘22

- 行番号: 1780-1785, 1866-1875, 1877-1884, 1483-1503, 1981-1989
- 現象: ポーズ中も難易度ボタンとCPU攻撃時の球種ボタンが操作でき、再開後の現在プレーの判定条件を変更できる。
- 再現条件: プレーヤー打席の投球中に`P`でポーズし、難易度ボタンをクリックしてから再開してスイングする。あるいはCPU攻撃の`aimPitch`中にポーズし、球種ボタンを押す。
- なぜ問題か: キーボードとキャンバスの操作は`isPaused()`で止めているが、UIボタンの`onclick`にはポーズ確認がない。難易度変更は次のスイング時に`DIFF_I`を読み直すため、投球開始後にタイミング幅・狙い幅を変えられる。表示の「ポーズ中」と、入力で変更できる試合条件が一致しない。
  > `if(isPaused()) return;                       // ポーズ中は操作を受け付けない`
  > `b.onclick=()=>{DIFF_I=i; ui();};`
  > `b.onclick=()=>{S.pitchSel=i; ui();};`
  > `const D0=isPlayerBatting()?DIFF[DIFF_I]:CPU_WIN;`
  > `const isPaused = ()=> paused || rosterOpen;`
- 修正案: ポーズ中は試合条件を変えるボタンを無効化し、再開・選手能力画面の開閉など許可した操作だけを残す。難易度変更を試合中に許可する仕様なら、少なくとも現在の投球へは次打者／次投球から適用する値を別に保持する。
- 重大度: 中

### 指摘23

- 行番号: 1435-1445, 1451-1458, 681-709, 1472-1478
- 現象: 満塁四球の走塁演出だけ、走者ETAを使わず1.7秒で終了し、三塁走者などを途中から記録位置へ瞬間移動させる。
- 再現条件: 満塁で4ボールになる。`beginRunners`が三塁走者を`p=3.12`、`goal=4.45`相当で開始し、`forcedWalk`は得点後の塁状態を直ちに確定する。
- なぜ問題か: 四球処理は`finishPlay`を通らず`S.timer=1.7`を固定し、通常の`runNeed`計算をしていない。1.7秒後の最後の0.45秒では、三塁走者がまだ本塁へ到達していなくても`r.p=r.goal`となる。得点は先に記録され、走者の物理表示だけが追いつかず、満塁押し出しの重要な境界が表示と判定でずれる。
  > `beginRunners(S.bases);                 // 押し出しの走塁を見せる`
  > `addRuns(r.runs); S.bases=r.nb;`
  > `S.phase='play'; S.timer=1.7; ui(); return;`
  > `runners.forEach(r=>{ if(r.p < r.goal){`
  > `if(S.timer<0.45) runners.forEach(r=>{ if(!r.out) r.p=r.goal; });`
- 修正案: 四球にも`runnerETA`を適用し、`S.bases`を確定した後の表示は全走者が目標塁へ着くまで保持する。早送りする場合は走者を記録済み位置へ直接表示する別演出にし、走行中の物理と混ぜない。
- 重大度: 中

### 指摘24

- 行番号: 646-674, 1150-1158, 1226-1236
- 現象: `planPlay`は野手が打球の中心座標まで走れるかで捕球可能性を決めるが、実判定は野手から4.5ft以内を捕球範囲としているため、先読みと実判定の距離モデルが違う。
- 再現条件: 打球の捕球候補点までの中心距離では野手が時間内に届かないが、実際の軌道は野手の4.5ft以内を通過するケース。`planPlay`の`F_REACT+runTime`が候補時刻をわずかに超える境界で起きる。
- なぜ問題か: `startFlight`は`plan.air`を`ball.canCatchAir`へ保存し、実判定はそのフラグが偽なら、実際に4.5ft以内へ入っても捕球条件を評価しない。つまり「グラブ半径を含めれば捕れる」野手を先読みが安打扱いに固定する一方、実判定側だけは4.5ftを掲げている。`planPlay`と`updateFlight`が同じ捕球モデルを使っていない。
  > `if(F_REACT+runTime(f.sp,Math.hypot(p.x-f.x,p.y-f.y)) <= p.t){ hit={t:p.t,p}; break; }`
  > `air: hit.p.z>1.2 && hit.p.t<=firstLand.t+0.02` 
  > `ball.pred=plan.firstLand; ball.primary=plan.f; ball.canCatchAir=plan.air; ball.planT=plan.t;`
  > `if(!ball.landed && ball.canCatchAir && ball.t>0.45 && ball.z>1.0 && ball.z<reach && d2<4.5 && (!closing || d2<1.2)){`
- 修正案: 先読み側も実判定側と同じ捕球半径を使い、距離から半径分を引いた到達時間を計算する。さらに中心到達でなく軌道区間と捕球球体の交差を先読みする。
- 重大度: 中

### 指摘25

- 行番号: 870-885, 889-898
- 現象: 送球が目標地点へ届かないまま5秒経過すると、受け手が実際に目標地点にいなくても捕球済みとして判定へ進む。
- 再現条件: 肩力・距離・中継野手の配置の組合せで、`far>maxD`なのに中継先`cut`が見つからない、または受け手が`T.dest`へ移動し切らない送球を発生させる。実環境での発生頻度は未検証で、低頻度経路の仮説を含む。
- なぜ問題か: 送球の到達条件が「距離5ft以内」または「T.t>5」になっている。後者はボールが目標へ届いたか、受け手がそこにいるかを確認せず`catch`へ遷移するため、5秒後にボールを受け手の手へ置き、塁上のアウト／セーフ判定だけを実行する。以前修正された「肩の届かない送球が途中で止まる」経路の最後に、物理未到達を判定到達へ変換する逃げ道が残る。
  > `if(far>maxD){`
  > `if(cut){ recv=cut; p=[cut.cx,cut.cy]; T.relayTo=T.target; }`
  > `if(d<5 || T.t>5){`
  > `T.stage='catch'; T.t=0;`
  > `T.catchDur = rnd(0.04,0.30)*(1.30-T.thrower.arm)*2.0;`
- 修正案: タイムアウトを捕球成功に使わず、未到達なら送球失敗・回収・追加中継へ遷移する。少なくとも`T.receiver`の実座標と`T.dest`の距離を確認し、物理到達を満たしたときだけ`judgeAtBase`を呼ぶ。
- 重大度: 中



## 監査対象版の変動記録

指定SHA版を開始時に確認し、全2,019物理行を読了した後、監査継続中にディスク上のHTMLが外部プロセスにより複数回変更された。中間版はSHA256 prefix `FCCD32BB07928EF3`、104,549 bytes、22:16更新、最終確認時の現物はprefix `4D656D1E265AD668`、105,257 bytes、22:18更新で、いずれも指定prefixとは一致しない。本レポートの行番号・引用・所見は変更前の指定SHA版だけに基づく。変更後版についての所見は、本監査の検証結果として扱わない。

## 最終評価: 既存Claudeテストハーネスの検出限界と矛盾確認

### 構造的に拾えない穴

- 2,592シナリオは、状態8種×アウト3種×打球27種×入力4種の離散的な機械状態を確認できるが、同じプレー中のキー押下順、キー長押し、`phase='play'`中の入力、ポーズ中のUIクリック、送球段階ごとの再指定を網羅しない。指摘2、17、19、21、22がこの穴に該当する。
- 固定`dt`実行は、`planPlay`の1/240秒先読みと実飛行の可変刻みの差、フレーム末尾だけの捕球／フェンス判定、背景タブのタイマー制限を再現しない。指摘4、16、20、24が該当する。
- 乱数を代表値で回すだけでは、捕球ミス遅延、ランダウンの送球ミス、CPUの遅いスイング、ファウル化などの低頻度分岐を網羅できない。乱数シードを固定して端点・発火条件を強制する必要がある。
- WebGLの実画面位置、ボールの瞬間移動、走者の表示位置、深度順序は、得点・アウト・runner conservationの数値検査だけでは観測できない。指摘5、6、9、12、17、18が該当する。
- 25試合は通常の9回進行と平均得点の確認には使えるが、同点を12回まで維持する試合、満塁四球、複数走者の離塁、第三アウト成立順、肩力・距離の境界を十分に含む保証がない。平均合計得点10.8はルール正しさの証拠ではない。

### 既存結果との矛盾

今回の所見は、提示された「全プレー終端」「アウト数上限」「得点範囲」「runner conservation」「第三アウトの得点」「ダブルプレー表示」「25試合の進行・守備交代・スコア整合」と直接矛盾しない。理由は、主な所見が表示と物理の不一致、入力順序、フレーム更新率、未実行の低頻度分岐、または12回同点の未到達分岐だからである。

ただし指摘10の混合アウト、指摘11の12回同点、指摘17の2アウト捕球前操作、指摘19の複数走者離塁を既存ハーネスが実際に生成していたなら、その該当結果とは矛盾する。その生成・操作ログは提示されていないため、ここでは「ハーネスが構造的に保証した範囲外」と判定する。既存ハーネスの0件という結果を否定するものではなく、0件から今回の表示・入力・実時間欠陥がないとは結論できない。





