# 守備判断レビュー（Codex独立精査）

対象: `_sol_defense_excerpt.js` 841行  
指示書: `_sol_task_defense_ai.md` 59行  
精査日: 2026-08-01

## 精査条件

- 指示書と対象コードをUTF-8で全行確認した。
- 対象は「コードは動くが、実際の野球なら別の守備行動・判定になる場面」に限定した。
- 命名、整形、性能、数値較正、指示書で検査済みの6項目は除外した。
- 各指摘は、条件を具体化してコードから動作を確定できるものだけを採用した。
- 対象コードは変更していない。既存の `sol_defense_review_20260801.md` も上書きしていない。

## 指摘

### 指摘1: 守備が走者の内部状態 `goal` を直接読んで判定する

1. **場面**: 1死一塁。遊撃手がゴロを捕って二塁へ送り、走者は `p=1.80`・`dir>0`・同じ速度で二塁へ進んでいるが、内部の `goal` だけが1と2で異なる場面。
2. **今のコードがする行動**: `judgeAtBase` は送球先へ来ている走者を `goal>=b` で選び、`updateRundown` は追う方向と切り返し先を `goal` で決める。したがって守備から見える動きが同じでも、内部目標だけで候補・追い込み方向が変わる。

   ```js
   608: const cand=runners.filter(r=>!r.out && r.p<b && r.goal>=b).sort((x,y)=>y.p-x.p)[0];
   629: const tagger = runners.filter(r=>!r.out && !r.mustReturn && r.p<b && r.goal>=b)
   638: const cand = runners.filter(r=>!r.out && r.p<b && r.goal>=b).sort((x,y)=>y.p-x.p)[0];
   524: const ahead={...r, p: clamp(r.p + (r.goal>r.p?0.09:-0.09), 0, 4)};
   537: if(Math.abs(r.p-r.goal) < 0.20){
   538: const goingHi = r.goal > (R.loLim+R.hiLim)/2;
   ```

3. **野球としての正しい行動**: 指示書の規約どおり、守備は `p`・`dir`・`v`・強制状態だけから、走者の進路と送球先を判断する。
4. **重大度**: **中**（通常送球から挟殺まで範囲は広いが、結果差が出るのは `goal` と実際の動きがずれた時）。


### 指摘2: 2アウトの非強制走者を、内野手が捕った瞬間に元の塁へ戻す

1. **場面**: 2アウト・三塁走者のみ・手動走塁指示なし。遊撃手がフェアのゴロを処理し、打者走者は一塁セーフになる場面（内野安打など）。
2. **今のコードがする行動**: 打球直後は2アウトなので三塁走者を本塁へ走らせるが、内野手が処理して `beginThrowPhase(..., 'infield', ...)` に入ると、非強制走者の自動目標を元の三塁へ戻す。そのため打者が一塁セーフでも、走者は本塁へ走り切らない。

   ```js
   703: if(window.__contactGo || S.outs>=2){ ball.forcedGo=true;
   708: runners.forEach(r=>{ if(r.origin>0){ setAutoGoal(r, Math.min(4, r.origin+1)); r.v=(r.sp||23)*0.5; } }); }
   273: if(kind!=='fly') runners.forEach(r=>{
   274: const auto = (kind==='outfield')
   275: ? safeReach(r, f)
   276: : ((r.origin===0 || isForced(r)) ? Math.min(4, r.origin+1) : r.origin);
   278: setAutoGoal(r, auto);                // 手動の指示はこの上に足される
   ```

3. **野球としての正しい行動**: 2アウトのフェア打球では全走者が走り切り、打者走者が一塁セーフなら三塁走者は本塁到達を目指す。
4. **重大度**: **中**（2アウトの内野安打時に得点を直接失う）。


### 指摘3: 2アウトでも、目の前の一塁自踏より二塁送球を優先する

1. **場面**: 2アウト・一塁。一塁手が一塁のすぐ近くでゴロを捕り、一塁自踏も二塁封殺も到達時間上は間に合う場面。
2. **今のコードがする行動**: `chooseThrowTarget` はアウト数・距離・成功余裕を比べず、刺せる候補のうち塁番号が大きい方を残すため、打者走者の一塁 `b=1` より一塁走者の二塁 `b=2` を選ぶ。自分で踏む判定も選択済みの `T.target` しか見ないので、一塁自踏へ戻らない。

   ```js
   221: if(m>0){                                    // ここなら刺せる
   222: if(!best || b>best.nb) best={nb:b};
   223: break;
   342: if(!T.stepChecked && T.target!=='P' && T.kind!=='fly' && T.kind!=='steal'){
   344: const tp0=throwPoint(T.target);
   348: if(my<30 && mine < cov + T.transfer){
   ```

3. **野球としての正しい行動**: 2アウトなら一塁手が目の前の一塁を踏み、最短・最安全の第3アウトを取る。
4. **重大度**: **中**（不要な長い送球でセーフや失策に変わり得る）。


### 指摘4: 全速で帰塁中の塁間走者を無視して投手へ返す

1. **場面**: 1アウト・二塁走者のみ。非強制の二塁走者が三塁方向へ出た後、二塁へ全速帰塁中で、三塁手が遅い内野ゴロを処理した時点では打者走者が一塁到達済み、他に進塁中の走者がいない場面。
2. **今のコードがする行動**: `chooseThrowTarget` は `dir<0` の帰塁走者を対象外にし、`trappedRunner` も速度が最高速の55%を超える走者を対象外にする。ほかの候補がなければ `'P'` を返し、帰塁走者を刺す・追い込む機会を捨てる。

   ```js
   204: const live=runners.filter(r=>!r.out && (
   205: r.dir>0 || (r.origin===0 && r.p<1) || (r.origin>0 && isForced(r) && r.p<r.origin+1)));
   259: if(frac < 0.30 || frac > 0.70) continue;
   260: if((r.v||0) > 0.55*(r.sp||23)) continue;           // 全速で走っている＝行き先は決まっている
   231: if(best) return {nb:best.nb};
   232: return {nb: fallback ? fallback.nb : 'P'};
   ```

3. **野球としての正しい行動**: 帰塁先への送球が間に合うなら送球し、間に合わないならボールを保持して走者を追い込み、無条件で投手へ返さない。
4. **重大度**: **中**（遅い内野打球で本来取れる走者アウトを失う）。


### 指摘5: 走者をタッチした瞬間にプレーを終え、続く併殺を捨てる

1. **場面**: 無死一塁。遊撃手が二塁寄りのゴロを捕り、走路上の一塁走者をタッチした後も、打者走者を一塁でアウトにできる場面。
2. **今のコードがする行動**: 保持者から3ft以内の走者をタッチアウトにした直後、次の送球先を再判断せず `concludePlay()` を呼ぶ。

   ```js
   314: if(Math.hypot(holder.cx-q.x, holder.cy-q.z) < 3.0){ markOut(r,'tag'); return r; }
   328: if(holder && T.kind!=='fly'){
   329: const tagged = tagNearbyRunner(holder);
   330: if(tagged){
   333: return concludePlay();
   ```

3. **野球としての正しい行動**: アウトが3つ未満で一塁送球が間に合うなら、タッチ後もプレーを続けて併殺を完成させる。
4. **重大度**: **中**（走路付近のゴロで併殺が単独アウトになる）。


### 指摘6: 強制進塁中でも、元の塁に触れていればタッチを無効にする

1. **場面**: 無死一塁。一塁手が一塁線のゴロを捕り、二塁へ強制進塁中だがまだ一塁に触れている走者へ、打者走者を先にアウトにする前にタッチした場面。
2. **今のコードがする行動**: `tagNearbyRunner` は強制状態を確認せず、整数の塁位置にいる全走者をタッチ対象から外す。

   ```js
   309: for(const r of runners){
   310: if(r.out) continue;
   311: if(Math.abs(r.p-Math.round(r.p))<0.035) continue;   // 塁の上にいる走者は触れてもセーフ
   ```

3. **野球としての正しい行動**: 打者走者に押し出された一塁走者は一塁の占有権を失っているため、強制が解除される前なら一塁に触れていても本人へのタッチでアウトにする。
4. **重大度**: **中**（タッチしてから一塁を踏む併殺などを失う）。


### 指摘7: 牽制球が塁へ着いただけで、離れた走者をタッチアウトにする

1. **場面**: 無死一塁。投手の牽制球を一塁手が一塁上で捕った時、走者は `p=1.10`（一塁から約9ft）までリードし、まだ走者本人には触れていない場面。
2. **今のコードがする行動**: 走者が塁より0.12塁間（10.8ft）を超えて先にいる時だけ挟殺へ移し、それ以内なら受け手と走者の実距離を見ず直ちに `markOut(cand,'tag')` とする。

   ```js
   598: const cand=runners.filter(r=>!r.out && r.jumped && Math.abs(r.p-b)>0.02)
   601: if(cand.p>b+0.12){ T.stealText='牽制で挟まれた！'; return beginRundown(cand, b+1); }
   602: markOut(cand,'tag'); T.stealText='牽制アウト！';
   ```

3. **野球としての正しい行動**: 牽制はフォースプレーではないため、一塁手が走者へ実際にタッチするか、届かなければ追って挟殺へ移る。
4. **重大度**: **中**（牽制で本来必要なタッチを省き、約10ftのリードまで即アウトにする）。


### 指摘8: 挟殺で実際にタッチしていなくてもアウトにする

1. **場面**: 1アウト一塁。投手の牽制球で走者が一・二塁間に挟まれ、切り返し後に二塁手がボールを保持しているが走者から3ft以上離れたまま、追い込み範囲が狭くなるか9秒を超えた場面。
2. **今のコードがする行動**: 3ft以内なら実タッチとしてアウトにする一方、それとは別に `hiLim-loLim<0.13` または9秒経過だけでも `markOut(r,'tag')` を実行する。

   ```js
   531: if(Math.hypot(R.holder.cx-rp.x, R.holder.cy-rp.z) < 3.0){
   532: markOut(r,'tag'); return endRundown('タッチアウト！','#95a3b4');
   535: if(R.hiLim-R.loLim < 0.13){ markOut(r,'tag'); return endRundown('タッチアウト！','#95a3b4'); }
   549: if(R.t>9){ markOut(r,'tag'); return endRundown('タッチアウト！','#95a3b4'); }
   572: if(R.t>9){ markOut(r,'tag'); return endRundown('タッチアウト！','#95a3b4'); }
   ```

3. **野球としての正しい行動**: 走路放棄など別のアウト理由がない限り、ボール保持者が走者へ実際にタッチするまで挟殺を続ける。
4. **重大度**: **中**（長引いた挟殺で、成立していないタッチアウトを作る）。


### 指摘9: 挟殺の両端へ同じ野手を二重に割り当てられる

1. **場面**: 1アウト二塁。三塁線のゴロを三塁手が捕って二・三塁間の走者を挟み、保持者以外では遊撃手が二塁にも三塁にも最も近い場面。
2. **今のコードがする行動**: `beginRundown` は三塁側と二塁側を続けて `rundownCover` へ依頼するが、どちらも除外するのは保持者だけである。最初に選んだ野手を予約しないため、2回目も同じ野手を選び、`coverBase` と移動目標を後の塁で上書きできる。

   ```js
   477: function rundownCover(base, exclude){
   480: fielders.forEach(f=>{ if(f===exclude) return;
   481: const d=Math.hypot(p[0]-f.cx,p[1]-f.cy); if(d<bd){bd=d;best=f;} });
   482: if(best){ best.coverBase=base; setTarget(best,p[0],p[1]); }
   507: rundownCover(hi, T.rd.holder);
   508: rundownCover(lo, T.rd.holder);
   ```

3. **野球としての正しい行動**: 挟殺の前後には別々の受け手を確保し、一方へ割り当てた野手をもう一方の候補から外す。
4. **重大度**: **中**（片側が無人になり、送球先を失う）。


### 指摘10: 挟殺の送球がそれると、回収せず自動的にセーフで終了する

1. **場面**: 1アウト一塁。投手の牽制から一・二塁間の挟殺になり、一塁手から二塁手への切り返し送球がそれたが、別の野手が回収できる、または走者がまだどの塁にも達していない場面。
2. **今のコードがする行動**: 送球前に `R.fumble` が立ち、送球の到達判定または3.2秒上限に達すると、走者位置・回収者・次の送球を見ず `endRundown('送球がそれてセーフ！')` でプレーを終える。

   ```js
   557: R.fumble = Math.random() < A.fumble(recv.cat)*2.2;
   582: if((Math.hypot(ball.x-R.dest[0], ball.y-R.dest[1])<5 && recvHere) || R.rt>3.2){
   583: if(R.fumble) return endRundown('送球がそれてセーフ！','#3fd66a');
   ```

3. **野球としての正しい行動**: 逸球後もボールは生きているので、守備は回収と再送球を続け、走者が塁へ達した時点で初めてセーフにする。
4. **重大度**: **中**（送球ミスのたびに、本来続くプレーを強制終了する）。


### 指摘11: フライ捕球後だけ、保持者の近接タッチを無効にする

1. **場面**: 無死一塁。一塁手が一塁付近の小フライを捕り、飛び出していた一塁走者が塁を離れたまま一塁手の3ft以内を通る場面。
2. **今のコードがする行動**: 保持者は存在するが、`T.kind==='fly'` の時だけ `tagNearbyRunner` を呼ばないため、その場のタッチアウトが成立しない。

   ```js
   325: const holder = (T.stage==='transfer'||T.stage==='step'||T.stage==='approach') ? T.thrower
   328: if(holder && T.kind!=='fly'){
   329: const tagged = tagNearbyRunner(holder);
   314: if(Math.hypot(holder.cx-q.x, holder.cy-q.z) < 3.0){ markOut(r,'tag'); return r; }
   ```

3. **野球としての正しい行動**: フライ捕球後でも、塁を離れた走者へボール保持者が触れれば、その場でタッチアウトにする。
4. **重大度**: **低**（内野の小フライと走者位置が重なる限定場面）。


### 指摘12: フライの離塁アピールアウトをフォースアウトとして記録する

1. **場面**: 1死一三塁。中堅手のフライ捕球で2死。三塁走者は正しくタッチアップして生還し、一塁走者は捕球前に離塁して、一塁へのアピールが第3アウトになった場面。
2. **今のコードがする行動**: 元の塁へ戻れなかった走者を見つける処理自体はあるが、アウト種別を `'force'` として記録する。

   ```js
   623: const back = runners.filter(r=>!r.out && r.mustReturn && r.origin===b && Math.abs(r.p-r.origin)>0.02)
   625: if(back && Math.abs(back.p-back.origin)>0.02){
   626: markOut(back,'force');
   627: T.flyText='離塁アウト！';
   ```

3. **野球としての正しい行動**: 捕球前離塁はフォースアウトではなくアピールアウトとして記録し、第3アウトなら各走者の本塁到達とアウト成立の時間順で得点を判定する。
4. **重大度**: **中**（頻度は限定的だが得点を変え得る）。ただし `markOut` と得点確定処理は抜粋外なので、実際に得点まで誤ることはこの抜粋だけでは断定しない。


### 指摘13: 壁反射後にカバー担当が打球処理へ回っても、代役を置かない

1. **場面**: 1アウト一塁。右翼手が追った右中間フェンス直撃球が内野方向へ大きく跳ね、再計算で二塁カバー中の二塁手が新しい捕球担当に選ばれた場面。
2. **今のコードがする行動**: 壁反射後は新担当を `primary` にして打球へ向かわせるだけで、保持していた `coverBase` を解除せず、空いた塁への代役も割り当てない。通常の地上球再計画にある再割当処理が、この分岐にはない。

   ```js
   733: const freed = plan2.f.coverBase;
   737: plan2.f.coverBase = null;
   739: if(freed!=null) rundownCover(freed, plan2.f);   // 空いた塁は別の野手が埋める
   792: const plan=planPlay(ball, true);       // 反射後は「今いる場所」から測り直す
   793: ball.primary=plan.f; ball.planT=ball.t+plan.t;
   795: fielders.forEach(f=>{ f.primary=(f===plan.f); if(f.primary) setTarget(f, plan.x, plan.y); });
   ```

3. **野球としての正しい行動**: カバー担当を打球処理へ回した瞬間に、その塁へ別の野手を再配置する。
4. **重大度**: **低**（壁反射後に新担当とカバー担当が重なる限定場面）。


### 指摘14: 塁状況・アウト数にかかわらず、守備位置を固定する

1. **場面**: 1死三塁で本塁封殺を狙う前進守備、無死一塁の併殺態勢、バント警戒、終盤の長打警戒など、同じ9人でも事前位置を変えるべき場面。
2. **今のコードがする行動**: `buildFielders` は `team` だけを受け取り、全野手を常に `POS_HOME` の固定座標へ置く。塁状況・アウト数・点差・回・打者の左右やバント警戒を読む分岐がない。

   ```js
   16: function buildFielders(team){
   17: return ROSTER[team].map(p=>{
   18: const h=POS_HOME[p.pos]||POS_HOME['投'];
   19: return {n:p.pos, name:p.n, x:h.x, y:h.y,
   21: cx:h.x, cy:h.y, tx:h.x, ty:h.y, primary:false, run:0, face:0, v:0};
   ```

3. **野球としての正しい行動**: 投球前に塁状況・アウト数・試合状況・打者特性から、前進、併殺深度、定位置、深め、バントシフトなどを選ぶ。
4. **重大度**: **中**（毎打席同じ配置になり、特定状況で本来取れるアウトや防げる得点を失う）。


## 重大度まとめ

| 重大度 | 件数 | 指摘 |
|---|---:|---|
| 高 | 0 | 該当なし |
| 中 | 12 | 指摘1〜10、12、14 |
| 低 | 2 | 指摘11、13 |

「高」は指示書の「毎試合起きて結果が変わる」を満たすと断定できるものに限定したため、該当なしとした。

## 該当なし・判定不能

- **通常打球のフォースとタッチの区別**: 643〜649行は、強制でない走者を塁踏みだけでアウトにせず挟殺へ回している。この分岐自体は該当なし。
- **フライ捕球そのものが第3アウトの場面**: 813〜824行は捕球時点で即終了し、得点を0にしている。この場面は該当なし。
- **一塁手が打球処理へ出た時の投手の一塁カバー**: 690行の候補順自体は該当なし。
- **通常の地上球で担当野手が交代した時のカバー再割当**: 731〜739行には再割当があるため該当なし。指摘13は壁反射分岐だけの漏れである。
- **捕球以外の第3アウトと得点成立順の最終結果**: `markOut`、`concludePlay`、`endRundown` の本体が抜粋外なので、指摘12のアウト種別誤記以外は判定不能。
- **併殺の第2送球条件**: 656行は現在の `runnerETA` と `throwETAof` を再比較していないが、能力値・到達可能な具体例をこの抜粋だけで固定できないため、独立した指摘には数えず未実装概念へ回した。
- **牽制先と別の塁の走者を候補にできる条件**: 598〜599行に `origin===b` はないが、複数走者が同時に `jumped` になれるかは生成処理が抜粋外なので、独立した指摘には数えなかった。
- **数値較正、例外、命名、性能、指示書で検査済みの6項目**: 対象外。

## 最後の1問: 守備判断として、そもそも実装されていない概念

この抜粋で不在を確認できる概念は次のとおり。

1. **投球前の状況別守備位置**: 前進守備、併殺深度、長打警戒、ライン際警戒、打者左右・走力・傾向による配置変更（16〜22行は固定 `POS_HOME`）。
2. **バント専用守備**: 一・三塁手のチャージ、投手・捕手の処理分担、二遊間の回転、ホイールプレー。抜粋内にバント専用分岐がない。
3. **打球側に応じた二塁カバー交代**: 二塁手と遊撃手のどちらが二塁へ入るかを打球方向・事前配置から選ぶ判断（692行は常に二塁手優先）。
4. **事前の中継位置取り**: 打球方向と狙う塁の線上へカットマンを動かす処理。現状は直接届かない時に、その場の距離だけで選ぶ（389〜394行）。
5. **カットする／通す／別の塁へ振る判断**: 後続走者を見て中継手が送球を止める、通す、三塁・二塁へ切り替える判断。
6. **二重中継とトレーラー**: 外野深部からの二人中継、先頭中継手の後ろに次の中継手を置く役割。
7. **送球先と打球の後方バックアップ**: 各塁後方、外野手同士、投手の本塁・三塁後方などへ別野手を回す配置（694〜697行、411〜413行）。
8. **アウト数・回・点差・サヨナラ状況による送球価値**: 1点阻止と確実なアウトのどちらを優先するかを試合状況で変える判断。`chooseThrowTarget` にこれらの入力がない。
9. **成功余裕と送球リスクの比較**: 「最も大きい塁番号」ではなく、距離、成功余裕、失策時の追加進塁から最も確実なアウトを選ぶ判断（221〜227行）。
10. **併殺第2送球の再評価**: 第1アウト成立後の現在位置から、打者走者への送球がまだ間に合うかを `runnerETA` と `throwETAof` で再計算する判断（656行は `fieldT<2.2` の固定条件）。
11. **最初の判定後も続くライブボール判断**: タッチ後の一塁送球、外野返球後の後続走者への送球、複数の離塁アピールなど。現状は狭い併殺分岐以外、633行・664行などでプレーを閉じる。
12. **実タッチ完了の状態管理**: 牽制、盗塁、挟殺で「送球先着」と「走者本人へのタッチ」を別に扱う仕組み（指摘7・8）。
13. **挟殺の役割予約とバックアップ**: 両端に別人を置き、送球の後ろにも補助を置き、逸球後に役割を組み直す仕組み（指摘9・10）。
14. **捕球担当交代後の全体ローテーション**: 新担当が空けた塁・中継・バックアップを全員で組み直す処理。通常の地上球は空いた一つの塁だけを局所補充し、壁反射では補充自体がない。
15. **フライの複数アピールと時間プレー管理**: 複数走者の早期離塁を順にアピールし、アピールアウトをフォースと分け、得点成立時刻と照合する仕組み。
16. **捕球優先権・声掛け・衝突回避**: `planPlay` は到達時間が最短の一人を選ぶだけで、内外野の優先権や声掛けがない（47〜63行）。
17. **走者を見て送球を保留する判断**: 投げずに保持して走者を釘付けにする、帰塁先へ投げる、先頭走者を戻してから別走者を見るといった継続判断。

## 検証記録

- 指示書: 59行、SHA-256 `39D82799F55C1A561FFE6657BD06246C7C6ADB93DDFAFCC4E86F13E139FABC8A`
- 対象コード: 841行、SHA-256 `A79641E96BD434E3DBA46DCEEC6E1DEF0AB9FB856EBD6AE6F26B23F7A920B0E4`
- 指摘数: 14件（高0・中12・低2）
- 対象コードの編集: なし


