# 【CC検証・2026-08-05】Sol検査便が「退行」とした2件（OI-140 / OI-238）は誤判定と確定

> 本文は `review_sol_20260805.md` の追補。Sol便の本体は改変していない。

この2件は**現行版（BUILD b0805-01）では起きない**。実機で機械検証した。

## 検証の方法

`_test_runner_reversal_20260805.js`（新規）を `baseball3d.html` の開発者コンソールで実行した。
0/1死・二塁走者だけを置き、二塁手が正面のゴロを捕球した瞬間 —— `beginThrowPhase(f2, 'infield', null, false)`
—— を直接作って、走者の行き先（`autoGoal`）がどうなるかを見た。

| 検証 | 走者の位置 | 期待 | 実測 | 判定 |
|---|---|---|---|---|
| 踏み切った走者は戻されない | p=2.5 | 3（三塁へ） | **3** | PASS |
| しきい値のすぐ上でも戻されない | p=2.40 | 3 | **3** | PASS |
| 塁上の走者は戻る（修理が行き過ぎていない） | p=2.05 | 2 | **2** | PASS |
| 2アウトなら未踏み切りでも進む | p=2.05 | 3 | **3** | PASS |

4件すべてPASS。**逆走は再現しない。**

3件目を入れているのは、修理が「常に進ませる」方向へ行き過ぎていないことを確かめるため。
塁にほとんど貼りついている走者（p=2.05）は設計どおり元の塁へ戻る。

## なぜ誤判定になったか（時刻で説明が付く）

| 時刻 | 出来事 |
|---|---|
| 12:39 | `beginThrowPhase` に `committed` 判定が入り逆走が直った（BUILD b0805-01、commit 07513995） |
| （この間） | Sol検査便が**修理前の版**を読んでレビュー |
| 15:22 | レビューファイルが更新された |

`baseball3d.html:1770-1778` に修理が入っている（Sol便が引用した 1766-1778 と同じ範囲）:

```js
/* ★2026-08-05修理: 既に踏み切った走者は戻さない。
   非封殺の内野ゴロで一律 r.origin へ戻していたため、打った瞬間に自動スタートした
   二塁走者が、捕球のフレームで三塁→二塁へ反転して逆走していた
   （録画20260805_105253で機械確認、OI-140は8/4・OI-238は8/5の同一事象＝退行）。
   塁を0.35以上離れていれば「戻るより進む方が近い」＝踏み切ったとみなす。 */
const committed = (r.origin>0 && r.p - r.origin >= 0.35);
```

塁を0.35以上離れていれば「戻るより進む方が近い」＝踏み切ったとみなし、`r.origin` へ戻さない。

Sol側も報告で「実行前に実装が b0805-01 へ、実行中にハーネスが別プロセスから更新された」と
述べており、**読み取り時点が修理をまたいでいた**ことと整合する。
指摘の中身（打球時と捕球時が競合する、という機構の説明）は正確で、誤っていたのは
「現存する」という時点の判定だけ。

## 残る論点

- 打球時（`baseball3d.html:2685-2695`）と捕球時（`1766-1778`）が**同じ走者の行き先を別々に決める構造自体は残っている**。
  今は `committed` で競合を避けているが、判断を1か所に集める形の方が素直。
  ただし現時点で不具合は出ていないので、作り直しは他の未解決を片付けてからでよい。
- Sol指摘のうち **OI-190 / 237 / 240**（担当交代時に旧担当の次役割まで一括更新しない）は別の真因で、
  こちらは**未検証**。OI-237 は録画不在のため経路が未確定。

## この検証から得た運用上の教訓

**レビューを外部へ出している最中に実装を直すと、レビュー結果と実装の版がずれる。**
今回は12:39に修理し、その後にSol便がレビューを完了している。
Sol側は自分で「実行中に別プロセスから更新された」と申告しており、
その申告があったからこそ版ずれを疑えた。

次から: 検査便を投げている間は実装を止めるか、止められないなら
**投入時点のコミットハッシュを検査便へ渡し、報告にそのハッシュを書かせる**。
そうすれば「どの版を見た指摘か」が機械的に分かる。
