# Fable 独立レビュー — オーナー指摘 OI-001〜OI-239 の解決判定

- 日付: 2026-08-05 / 対象: `baseball3d.html` BUILD `b0804-33`（4,291行）／`_test_harness_20260804.js`
- 依頼: `OWNER_ISSUE_MASTER_20260805.md` 第6部の発注仕様に従う
- 最上位目的: **オーナーが遊んでいてバグに出会わないこと**。所見はこの目的への寄与/毀損を先に書く
- レビュー担当は実装を一切変更していない。変異テストはブラウザのメモリ内でのみ関数を差し替え、リロードで復元した（ファイル不変更）
- 深さの申告: 本レビューの主武器は「照合」（コード全読み＋逐語引用）。中心 4 件（OI-238 再現・ハーネス全実行・変異テスト・回帰機構）は「実行」。全239件を「実行」で確かめてはいない（各行の深さ欄が正）

---

## 0. 中心結論（先に結論）

### 0-1. OI-238 の真因（実行で確認済み・最重要）

**走者の行き先を決める権限が2か所にあり、捕球の瞬間に後者が前者を黙って上書きする。**

- 権限A（打った瞬間）: `startFlight` L2685-2695。オーナー仕様 OI-141 を実装した自動スタート。二塁走者は「遊・三・投・捕の正面ゴロ以外なら」三塁へ走り出す:
  ```js
  else if(r.origin===2) go = !'遊三投捕'.includes(pn);
  ...
  if(go){ setAutoGoal(r, Math.min(4, r.origin+1)); r.v=(r.sp||23)*0.5; }
  ```
- 権限B（野手が捕った瞬間）: `beginThrowPhase` L1766-1780。kind='infield'（内野ゴロ）の場合、**押し出されていない走者は無条件で元の塁へ戻す**:
  ```js
  let auto = (kind==='outfield')
    ? safeReach(r, f)
    : ((r.origin===0 || isForced(r) || baseOuts()>=2) ? Math.min(4, r.origin+1) : r.origin);
  ```
  0死・走者二塁のみ（＝押し出しなし）の二塁ゴロでは `auto = r.origin = 2`。`floorBase = Math.max(r.origin, Math.floor(r.p+1e-9))` も p=2.5 なら 2。よって `setAutoGoal(r, 2)`（L1778）で **goal が 3→2 に反転**する。手動 cmd が無い（キー未押下）ので `setAutoGoal` はそのまま goal を書く（L4231-4236 `if(!force && r.cmd) return; r.goal=g;`）。
- **実行で再現**: b0804-33 のライブ実行（0死・二塁・92mph/-3°/方向10°）で、`throwPlay.stage` が `transfer` になったフレームに走者 goal が 3→2 へ反転（p=2.44・v=24ft/s で三塁へ疾走中に反転）。オーナー録画 `20260805_105253.json` の f=15 `2:2.5→2` と完全一致。
- **決定的な矛盾（doc-code gap）**: 権限Aの直前のコメント L2673 は「行けるかどうかは野手が捕った時点で safeReach が決め直す（無理なら戻る）」と書くが、**safeReach が使われるのは kind='outfield' だけ**（L1770-1771）。内野ゴロでは safeReach を通らず「戻る」既定に落ちる。コメントが約束した設計が実装されていない。
- OI-140（8/4 06:26、同一事象）は指摘マスターどおり**対応記録なし＝一度も直っていない**。よって判定は「退行」ではなく **未解決（2回目の被弾）**。
- 修正方針の示唆（判定のみ・実装は別担当）: 権限Bの infield 分岐で、走者が既に走り出している（`r.v` が大きい／p が塁の中間を越えた）場合は safeReach か「継続」を使う。少なくとも権限Aが立てた go 判断を捨てない。

### 0-2. OI-237 の候補経路（録画が無いため判定不能。ただし経路は特定した）

「ライトが打球を追っている途中で不自然に追うのを一瞬やめた」を起こせるコード経路は3つ実在する:

1. **担当再割当のフリッカー（最有力）**: `stepFlight` L2753-2769。着地後 0.35 秒ごとに `planPlay(ball, true)` で最速野手を選び直す。**現職への引き留め（ヒステリシス）が無い**ため、右翼手と中堅手の到達見込みが拮抗するギャップの打球では担当が往復しうる。担当を失った瞬間の旧担当（右翼手）には**新しい目標が与えられず**（L2761 は primary フラグを外すだけ）、古い目標に着けば `moveFielders` L1413 `else if(d<=0.5){ f.v=0; }` で立ち止まる。0.35 秒後に再び担当に戻れば走り直す＝「一瞬やめてまた追う」。なお速度も非担当は 0.88 倍（L1410）。
2. **壁当て後の再計画**: L2843-2849。フェンス反射の瞬間に `planPlay(ball, true)` で担当を取り直す。ツーベース（壁直撃）ではまさにこの瞬間に 1. と同じ非担当降格が起きうる。
3. **飛び込み失敗のスタン**: L2893-2895 `if(dive01>0.30) prim.stun=0.25+0.5*dive01;`。スタン中は `moveFielders` L1407 で完全停止。ただしこれは倒れ込みの見た目が付くので「不自然」とは見えにくい。
- OI-190 の修正（`rundownCover(freed, plan2.f, prim)` L2767 で旧担当をカバーに引き抜かない）は「旧担当が塁へ走り出す」事象だけを塞いだ。**「旧担当が何の指示も持たず立ち往生する」「担当が往復する」は塞がれていない**。
- 再現に必要なもの: G録画（直近60プレー）でツーベース時の `fld` 列の `*`（担当マーク）が右翼→他→右翼と移る瞬間を見る。

### 0-3. S-13「直したはずが再発する」の機構（本依頼の中心）

ビルド刻印（旧ビルド録画）で説明できない再発は**実在する**。OI-238 がその現物（b0804-33 で再現）。機構は3つ:

1. **同じ量に対する複数の書き手（適用条件のない上書き）**: 走者の goal は (a)startFlight の自動スタート (b)beginThrowPhase の再決定 (c)updateRundown (d)applyRunnerKeys (e)resolveCatch の少なくとも5か所が書く。OI-220 で立てた規律「場面パッチには必ず適用条件を付ける」は chooseThrowTarget の転送（L1714 `const isOF = Math.hypot(f.cx,f.cy) >= 158;`）には適用されたが、**beginThrowPhase の走者リセットには適用条件（「走者が既に踏み切っているか」）が無いまま残った**。片側（打った瞬間）だけを直す修正は、もう片側（捕った瞬間）が黙って打ち消す——これが「自動スタートを実装したのに勝手に戻る」が 8/1（OI-089）→8/4（OI-140）→8/5（OI-238）と3回出続けた理由。
2. **回帰テストが「直した事象」しか固定しない**: ハーネス13本を b0804-33 で全実行し**全PASSを確認**（CC申告どおり）。しかし**同じビルドで OI-238 が1発で再現**する。テストは過去に直した経路（挟殺・塁回り減速・幽霊飛行…）を監視するが、**走者の goal の一貫性（打った瞬間の判断が捕球で消えないこと）を見るテストは1本も無い**。さらに変異テスト（下記 §3）で、S-7（本塁未到達の得点）を意図的に再導入してもハーネスが検出できるかを実測した。
3. **較正と守備ロジックの結合によるfixture腐敗**: 8/5未明に3件発見済み（test1/2/3）。腐敗は「テストが偽のFAILを出す」形で顕在化したが、より危険なのは**偽のPASS**（意図した場面がもう再現されておらず検出力ゼロのまま合格が出る）。test4（叩きつけ30試行）・test9（15試行）は場面生成を実プレーに依存しており、守備較正が進んで「そもそもバウンド後の捕球場面が発生しない」状態になっても PASS を出し続ける構造（bad件数=0 と 場面未発生=0 を区別しない）。test4 は `airCatchAfterBounce` の分母（bounced になった試行数）を出力していないため、腐敗しても気づけない。

### 0-4. 判定の内訳（全239件）

> ★同日の追加指摘 OI-240（§7）・OI-241（§8）を加えた**最終集計は §8-3**（241件、未解決7）。本表はレビュー第1便（OI-239まで）時点の値。

| 判定 | 件数 |
|---|---|
| 解決 | 149 |
| 部分解決 | 30 |
| 未解決 | 5 |
| 退行 | 0 |
| 判定不能 | 30 |
| 対象外（メタ指示・設問・設定変更・受領記録） | 25 |

- 未解決 5 件: **OI-140・OI-238**（走者反転。同一真因、実行で再現）、**OI-141**（オーナー仕様は実装されたが OI-238 の上書きで実効を失っている）、**OI-169**（打撃・投球・守備の3系統別難易度が存在しない）、**OI-171**（送球のスタンドイン＝2個の安全進塁規則が存在しない）。
- OI番号の外では **Sol P3**（離塁10.8ft容認）が未解決のまま現存（§1）。
- 「退行」0件の意味: 「一度コードで塞いだものが後の変更で別経路から復活した」ことを行番号で立証できた例は無かった。オーナーの再発体験の主対は (a)旧ビルド録画（ビルド刻印以前）と (b)**最初から塞がっていなかったもの**（OI-140→238 型）で説明がつく。ただし全較正値の時系列比較はしていないので、較正の揺り戻しを退行として検出することは本手法ではできない（§5）。

---

## 1. 外部レビュー未追跡分の決着（Sol P1〜P6 / Codex）

| 項目 | 判定 | 根拠 |
|---|---|---|
| Sol P1 本塁未到達でも行き先が本塁なら得点 | **部分解決** | 主経路は修理済み: `judgeAtBase` L2387 `if(b==='P') return concludeOrChaseHome();`、`concludeOrChaseHome` L2338 `const homing=runners.find(r=>!r.out && r.goal>=3.99 && r.p<3.97 && r.p>2.0);` で本塁走行中は続行。`markOut` L2485-2486 のタイムプレー（`S.thirdOutScored`）と `concludePlay` L2541-2543（第三アウトが force/fly なら `runs=0`）も防御。**残存経路**: `updateThrowPhase` L1841-1843 の 12 秒打ち切り `if(S.playClock > (inRundown?24:12)){ ... return concludePlay(); }` から `concludePlay` L2513 `let g=Math.round(r.goal); if(g>=4){ ... runs++ }` に入ると、**位置でなく行き先の丸めで得点する**。12秒続くプレーは稀だが経路は生きている。深さ=照合 |
| Sol P2 タッチしていないのに盗塁アウト | **部分解決** | 送球先の判定は受け手が塁に居る事が前提になった（L2127-2131 受け手が塁へ着くまで判定保留）が、`judgeAtBase` steal 分岐 L2365-2366 `if(cand && runnerETA(cand,b)>0.02){ markOut(cand,'tag'); ... }` は**受け手と走者の距離を見ずに** ETA だけでタッチアウトにする。走者がまだ 20ft 手前でも即アウト。挟殺側にも L2202-2203 `if(R.hiLim-R.loLim < 0.13 && Math.hypot(...) < 6.0){ markOut(r,'tag'); ...}` と**6ft 距離での自動タッチ**が残る（通常タッチは 3.0ft: L1825/L2196）。深さ=照合 |
| Sol P3 約10.8ft以内の早期離塁を塁上扱い | **未解決（現存）** | `resolveCatch` L2998 `if(r.p > r.origin+0.12){` （0.12塁=10.8ft）。それ以下は L3002 `r.p=r.origin;` と**塁へスナップ**され帰塁義務を負わない。リード既定は +0.085（7.65ft、L1338）なので通常リードは救済されるが、10.8ft までの飛び出しが無条件に免罪される構造はそのまま。深さ=照合 |
| Sol P4 一人の挟殺終了で別走者ごとプレー終了 | **部分解決** | `endRundown` L2312-2313 は**本塁へ向かう走者（p>2.0）だけ**続行（`const homing=runners.find(r=>!r.out && r.goal>=3.99 && r.p<3.97 && r.p>2.0);`）。一・二塁間を走る別走者は `concludePlay` に落ち、L2512-2524 で goal の丸め先へ確定される（見た目は走って合わせるが送球での勝負は起きない）。タッチ経路（L1874-1886）には `chooseThrowTarget` での続行があるのに、挟殺決着経路には本塁以外の続行が無い＝**片側だけの修理**。深さ=照合 |
| Sol P5 S/X切替の瞬間に守備が送球先を変える（=OI-056） | **部分解決** | `chooseThrowTarget` は宣言どおり内部目標を読まない（L1621-1622 コメント「走者が『どこへ行くつもりか』は見えないので使わない」、実装も r.dir/r.p/r.v のみ）。**しかし `r.dir` は L1356 `r.dir = r.p<r.goal-1e-6 ? 1 : (r.p>r.goal+1e-6 ? -1 : 0);` ＝ goal から毎フレーム導出される**。プレイヤーが X を押した瞬間に goal が変わり、走者が物理的に向きを変える前（速度はまだ前向き）でも dir は同フレームで反転する。送球直前の再判断（L1917）がこの dir を読む＝**「意図の即時漏洩」は形を変えて残存**。v による sprinting 判定（L1665）だけが遅延を持つ。深さ=照合 |
| Sol P6 走行距離が試合中ずっと肩へ累積 | **解決** | `armEff` L1449 `const run = Math.min(1, (f.run||0)/70);` の f.run は `resetFielders` L3094 `f.primary=false; f.face=0; f.v=0; f.stun=0; f.fumbled=false; f.run=0;` で**毎プレー後にリセット**される（`afterPlay` L3165 が毎打席呼ぶ）。プレー内の累積は姿勢表現として意図どおり。深さ=照合 |
| Codex 14件+17概念 | **部分解決** | 14件の中核（走者内部目標の参照→P5参照/2アウト時送球判断→L1703 `const pick=(baseOuts()>=2)?(sure||lead):(lead||sure);`/タグ・フォース混同→L2419-2428 forceable 判定/挟殺自動アウト→L2200-2203「実際にタッチしていなければアウトにしない」（ただし6ft残存）/ベースカバー→L2646-2659）は対応済み。17概念は**前進守備（L3086-3093）とカットオフ（L1978-1989）の2つのみ実装**。バントシフト・二塁牽制・申告敬遠・中継の「切る」判断などは未実装（charter も自認）。深さ=照合 |

---
## 2. 全239件の判定

> 形式: `OI番号 | 判定 | 根拠（file:line と逐語引用/要点） | 再現条件 | 深さ`
> 発注仕様の5判定に加え、不具合判定が意味を持たない行（設問・設定作業・メタ指示・外部レビュー受領）には `対象外` を使った（黙って5判定に押し込むより正直なため。全行を出し、黙って落とした行は無い）。
> 「解決」は行番号つきでコード上の閉塞を示せたもの。体感・頻度・見た目の合否はコードからは決められないため `判定不能` とし、何を実行すれば決まるかを書いた。file はすべて `baseball3d.html`（別ファイルは明記）。

### 第1部 2026-07-31（OI-001〜072）

| OI | 判定 | 根拠 | 再現条件 | 深さ |
|---|---|---|---|---|
| 001 | 対象外 | ゲーム発注。現行 `baseball3d.html` が成果物 | - | 照合 |
| 002 | 判定不能 | 旧2D版(`baseball.html`)の起動不具合。現行3D版に該当箇所なし | 旧版起動 | 照合 |
| 003 | 対象外 | 3D化の発注。実装済み | - | 照合 |
| 004 | 解決 | L938-942 `const DIFF=[{name:'やさしい',...cpu:0.70,...}]` 3段階＋L949 `const SPD=[1.62,1.34,1.10]`（球速緩和） | - | 照合 |
| 005 | 判定不能 | 表示見切れは実行環境依存 | 実機目視 | 照合 |
| 006 | 解決 | L952-957 `{name:'カーブ', dur:0.70, bx:-1.25, by:-2.70,...}` 変化量が実装され pitchPos が補間 | 体感確認は実機 | 照合 |
| 007 | 解決 | L842-847 `'一':{x:52,y:82}, '二':{x:34,y:143}, '遊':{x:-34,y:143}, '三':{x:-52,y:82}` と L484 一塁=(+63.64)。一塁側=+x で整合、左右逆はない | - | 照合 |
| 008 | 対象外 | 品質メタ要求 | - | - |
| 009 | 判定不能 | 難易度別の到達点マーク L942 `mark:0.18〜0.62` が補助。体感の合否は実機 | 実機プレー | 照合 |
| 010 | 解決 | L949 `const SPD=[1.62,1.34,1.10]`（難易度で1.10〜1.62倍に減速） | - | 照合 |
| 011 | 解決 | OI-006と同じ | - | 照合 |
| 012 | 解決 | L2831-2832 `if(ball.z>9.4){ finishPlay(ball.landed ? resolveHit(4.6,...,true) : resolveHomer()); }`＝柵越えのみエンタイトル/HR、それ以外は L2835-2841 反射 | - | 照合 |
| 013 | 判定不能 | L1076 `const aimDist=Math.hypot(dx*0.60, dy*1.30);` でカーソルずれは打球に反映される設計。見た目との一致は実機 | 実機プレー | 照合 |
| 014 | 解決 | L2854-2883 最接近方式 `const receding = inAir && d2 > (ball.dPrev===undefined ? 1e9 : ball.dPrev);`＋`const d3 = Math.hypot(d2, Math.max(0, ball.z-4.2));` 高さ込み距離で捕球。横を素通りした球は捕れない | - | 照合 |
| 015 | 判定不能 | L887 `FIELD_SP={一:0.84,...右:0.95}` で守備<走塁は担保。ただし「プロ実タイム」との突合記録なし（S-12） | CPU守備の実測 | 照合 |
| 016 | 解決 | OI-012と同じ（L2829-2830 コメントに旧不具合の経緯明記） | - | 照合 |
| 017 | 解決 | OI-014と同じ | - | 照合 |
| 018 | 解決 | beginRunners L1322-1341 | - | 照合 |
| 019 | 解決 | updateThrowPhase L1829-2136（transfer/fly/catch/step/approach/rundown） | - | 照合 |
| 020 | 解決 | L1390-1395 `const max=fenceDist(clamp(...))-2; return d>max ? [x/d*max, y/d*max] : [x,y];`（守備は柵外へ出られない） | - | 照合 |
| 021 | 解決 | applyRunnerKeys L3958-4045＋setManualThrow L4216-4221 | - | 照合 |
| 022 | 解決 | buildFielders L923-929 `sp:A.speed(p.走)*..., zone:A.zone(p.守), arm:A.arm(p.肩), fld:p.守, cat:p.捕, acc:(p.送...)` | - | 照合 |
| 023 | 判定不能 | charterに出荷状態の機械計測（あなた9.3点/CPU2.5点）の記録あり。本レビューでは再測定していない | CPU対CPU10試合 | 照合 |
| 024 | 解決 | L1797-1800 `transfer: A.transfer(f.fld,f.cat) + rnd(0,0.10) + (...rnd(0.55,1.15):0)` | - | 照合 |
| 025 | 解決 | markOut L2480-2481 `if(kind!=='fly' && (S.phase==='flight'||S.phase==='throwing')) setMsg('アウト！',...)`＝成立瞬間の表示。打ち切りは別 | - | 照合 |
| 026 | 部分解決 | L1441-1446 距離別出力＋L863 `arm: v => 0.33 + 0.0074*(v-1)`（Statcast幅）＋stepBall空気抵抗。体感合否は未検証 | 実機プレー | 照合 |
| 027 | 解決 | ROSTER L893-897（8能力）＋L864 `transfer: (fld,cat) => 0.55 + 0.28*(1-fld/100) + 0.26*(1-cat/100)`＝オーナー仕様どおり守備力+捕球の両依存 | - | 照合 |
| 028 | 解決 | drawRoster L4187-4210（送球列 L4198 含む） | - | 照合 |
| 029 | 解決 | L4258-4259 `const isPaused = ()=> paused || rosterOpen;`（R画面中は自動ポーズ） | - | 照合 |
| 030 | 判定不能 | OI-015と同じ（S-12） | - | 照合 |
| 031 | 対象外 | OI-033で操作系が上書きされた | - | - |
| 032 | 解決 | S/X 2キー方式 L3958-4045 | - | 照合 |
| 033 | 解決 | L4058-4061（S/Z/X/C配置）＋held L3892-3896 | - | 照合 |
| 034 | 解決 | L2419-2428 `const forceable = (cand.origin===0) ? (b===1) : (isForced(cand) && b===cand.origin+1); if(!forceable) return beginRundown(cand, b);` | - | 照合 |
| 035 | 解決 | beginRundown/updateRundown L2165-2292 | - | 照合 |
| 036 | 解決 | L2646-2659 打った瞬間の全塁カバー割当（`put(1, ['一','投','二'])` 等） | - | 照合 |
| 037 | 解決 | held方式 L3892-3896＋毎フレーム反映 L3304 | - | 照合 |
| 038 | 解決 | resolveCatch L2996-3010（mustReturn/タッチアップ） | - | 照合 |
| 039 | 解決 | L2720-2724 `if(ball.z<=0.02) ball.landed=true;`（t>0.2ガード撤去、撤去理由コメント併記）。test4を実行し30試行違反0 | - | 実行 |
| 040 | 解決 | resolveCatch L3013-3015 捕球瞬間に表示 | - | 照合 |
| 041 | 解決 | chooseThrowTarget L1626-1718（間に合う見込みのある塁しか勝負しない） | - | 照合 |
| 042 | 部分解決 | OI-026と同じ | - | 照合 |
| 043 | 解決 | L1371 `if(r.p>=r.goal-1e-9 && r.cmd==='S') r.cmd=null;`（塁到達で指示消費） | - | 照合 |
| 044 | 解決 | L1425-1427コメント＋L1944-1945（投手への返球は投手の現在地へ） | - | 照合 |
| 045 | 解決 | finishPlay L3130-3145 `runNeed`（走者が走り終わるまで表示保持。旧「90秒待ち」バグの経緯もコメントに記録） | - | 照合 |
| 046 | 解決 | concludePlay L2512-2524（実際の到達塁で `nb` を再構成し次打席へ渡す） | - | 照合 |
| 047 | 解決 | L1925-1929 `if(T.target!=='P' && myStep < 6){ ... T.stage='step'; }`（目の前の塁は自分で踏む）＋L2127-2131 受け手到着まで判定保留・受け手は塁へ歩く | - | 照合 |
| 048 | 解決 | L2911-2920 `if(baseOuts()+1>=3){ ... finishPlay({...}); return true; }`（捕球3アウトで即終了・併殺表示なし） | - | 照合 |
| 049 | 対象外 | ハーネス・外部レビュー体制の起源 | - | - |
| 050 | 解決 | L2989-2991 `const kind = ball.maxZ<7 ? 'ゴロ' : (...)`＝実際の最高高さで呼称 | - | 照合 |
| 051 | 部分解決 | 送球先判断は全面書換 L1626-1718。ただし中継の「切る」判断は relayTo 固定（L2116-2120）で未実装（charter自認） | 中継絡みの実機 | 照合 |
| 052 | 解決 | held方式＋L1371 | - | 照合 |
| 053 | 解決 | drawHUD L3797-3806（塁図） | - | 照合 |
| 054 | 解決 | held方式（押している間だけ）L3892-3896 | - | 照合 |
| 055 | 解決 | 同上（L3892 コメント「今キーが押されているかで判定」） | - | 照合 |
| 056 | 部分解決 | §1 Sol P5 参照。chooseThrowTarget は goal/cmd を読まないが、L1356 `r.dir = r.p<r.goal-1e-6 ? 1 : (r.p>r.goal+1e-6 ? -1 : 0);` が goal 由来のため S/X の瞬間に dir が反転し、判断へ同フレームで漏れる | X押下の瞬間に送球先が変わるか実機 | 照合 |
| 057 | 部分解決 | OI-051と同じ | - | 照合 |
| 058 | 解決 | applyRunnerKeys L4013-4032（走者ごとの独立判定） | - | 照合 |
| 059 | 解決 | L4037-4044（帰塁も個別）＋1-3キー個別指定 L3970-3971 | - | 照合 |
| 060 | 解決 | safeReach L1720-1737（実際の足と送球の競争）＋外野安打も judgeAtBase L2409-2417 で判定 | - | 照合 |
| 061 | 解決 | OI-025と同じ | - | 照合 |
| 062 | 解決 | step stage L2106-2110（踏んでから judgeAtBase）。併殺転送は outMade 後 L2435-2441 | - | 照合 |
| 063 | 対象外 | メタの問い→§0-3が回答 | - | - |
| 064 | 解決 | ROSTER[0] L899-907 全員 `ミ:100,...走:100`（全能力100） | - | 照合 |
| 065 | 解決 | L1703 `const pick = (baseOuts()>=2) ? (sure||lead) : (lead||sure);`（2死は確実な塁を優先） | - | 照合 |
| 066 | 解決 | L2214-2223（受け手が塁に入るまで投げない・自分が近ければ踏みに行く） | - | 照合 |
| 067 | 部分解決 | 封殺カバー修正の機構は L2122-2131。charterにゴロ安打率69%→29%の測定記録。本レビュー未再測定 | 10試合実測 | 照合 |
| 068 | 部分解決 | 継続改善中（charter未解決3の残課題あり） | - | 照合 |
| 069 | 解決 | L4026-4029 `const max = (ahead && ahead.goal<4) ? ahead.goal-1 : 4;`（本塁は詰まらない） | - | 照合 |
| 070 | 解決 | L1628-1633 封殺列挙に押し出し走者を含む＋lead優先 L1687-1694。本塁マージン0.35秒(L1685)が保守的な点は charter 自認の残チューニング | 満塁ゴロ実機 | 照合 |
| 071 | 解決 | updateStealCommands L3899-3947（重盗対応） | - | 照合 |
| 072 | 解決 | L1058 `let batMode = 0;`＋バント L3356-3361 | - | 照合 |

### 第2部 2026-08-01〜08-02（OI-073〜127）

| OI | 判定 | 根拠 | 再現条件 | 深さ |
|---|---|---|---|---|
| 073 | 部分解決 | S-1多層防御: L1852-1858 allSettled＋L2495-2500 holdOpen＋L3115-3117 最終ガード＋sweep塁間チェック。残存: L1841-1843 の12秒打ち切りは走者位置を見ずに concludePlay へ入る | 12秒超の長いプレー | 照合 |
| 074 | 解決 | L2922-2928（走者なしフライは `quick:1.3` で即終了） | - | 照合 |
| 075 | 解決 | resolveHomer L2977 `quick:1.6` | - | 照合 |
| 076 | 解決 | L2668-2669 `if(window.__contactGo || S.outs>=2){ ball.forcedGo=true;`＋test7 実行PASS | - | 実行 |
| 077 | 部分解決 | §1 Sol P2 参照（L2365-2366 はETAだけでタッチアウトにする） | 盗塁の際どい場面 | 照合 |
| 078 | 解決 | L1155 `if(b.vz<-1.2){ b.vz=-b.vz*0.55; b.vx*=0.78; b.vy*=0.78; }` | - | 照合 |
| 079 | 判定不能 | doSwingにモード別分岐は実装。打球分布の合否は統計測定が要る | 分布測定 | 照合 |
| 080 | 部分解決 | OI-073と同じ | - | 照合 |
| 081 | 解決 | L2653 `put(1, ['一','投','二']);`（打った瞬間に割当）＋L1402-1405 カバーは反応待ちなしで全力 | - | 照合 |
| 082 | 解決 | L861 `speed: v => 23.0 + 0.0758*(v-1),`（コメント: 走力50で26.7=平均、実測合わせ） | - | 照合 |
| 083 | 解決 | L1330-1333 `sp:A.speed(bat.走)*rnd(0.94,1.0)`（毎回ぶれる） | - | 照合 |
| 084 | 解決 | OI-082と同じ（1〜100全域を実測レンジに線形対応） | - | 照合 |
| 085 | 解決 | L3902-3910 earlyHold→`S.jumpEarly=true`→beginPickoff L3233（押しっぱなし盗塁は牽制で刺される） | - | 照合 |
| 086 | 解決 | L3916-3919 投球中も applyRunnerKeys（S/X・追い越し禁止・個別）＝重盗・途中帰塁可 | - | 照合 |
| 087 | 解決 | L2172-2174 `const hiCover=rundownCover(hi, T.rd.holder); rundownCover(lo, T.rd.holder, hiCover);`（両側に別野手） | - | 照合 |
| 088 | 解決 | Z=先頭のみ L3963-3966＋1-3キー指定 L3967-3971 | - | 照合 |
| 089 | 部分解決 | 自動スタート自体は L2666-2696 に実装。**ただし押し出されていない走者は捕球の瞬間に OI-238 の反転で戻される＝実効が失われている**（§0-1） | 0死二塁の内野ゴロ | 実行 |
| 090 | 部分解決 | OI-089と同じ | - | 実行 |
| 091 | 解決 | L2247-2250 `const gBefore=r.goal; setAutoGoal(r, away); if(r.goal!==gBefore) r.v=0;`（向きが変わった時だけ減速） | - | 照合 |
| 092 | 部分解決 | L863 arm係数（内野80mph級/外野88mph級は charter 記録）。体感未検証 | - | 照合 |
| 093 | 解決 | L3260-3265 `transfer: 0.88 + 0.22*(1-cat.cat/100) + rnd(0,0.20),`（ポップタイム実測2.0秒合わせ） | - | 照合 |
| 094 | 解決 | L2233-2237 `if(R.fumble){ ... dest=[p[0]+o[0]*2.4, p[1]+o[1]*2.4]; }`（乱れた球は実際にそれる） | - | 照合 |
| 095 | 解決 | armEff L1448-1455（走行距離・守備位置係数）＋throwOffset L1463-1467 | - | 照合 |
| 096 | 部分解決 | L1453-1454 `const pos = (f.n==='捕') ? 0.88 : (f.n==='投' ? 0.86 : ...)` 係数は存在。捕手・投手の肩の個別実測検証は記録がない（S-2未確認点） | 捕手・投手の送球実測 | 照合 |
| 097 | 解決 | L2647-2650 assignedセットで1人1塁＋primary除外（二重に踏みに行かない） | - | 照合 |
| 098 | 解決 | trappedRunner L1741-1759＋approach L2084-2103（持って詰めて挟殺へ） | - | 照合 |
| 099 | 解決 | L1058 `let batMode = 0;      // 既定はミート打ち` | - | 照合 |
| 100 | 対象外 | 依頼→外部レビュー取得済み。消化状況は§1 | - | - |
| 101 | 解決 | L2658 `put(2, (c.spray>5 ? ['遊','二','投','一'] : ['二','遊','投','一']));`（当該場面の対処） | - | 照合 |
| 102 | 判定不能 | S-12。現行値の実測記録なし | 実測 | 照合 |
| 103 | 解決 | L4155/L4163/L4172（難易度・球種・打ち方ボタンがポーズ中も有効、コメント明記） | - | 照合 |
| 104 | 解決 | L1746-1752 `if(frac < 0.30 || frac > 0.70) continue; if((r.v||0) > 0.55*(r.sp||23)) continue; ... > 80) continue;`（発動条件を限定） | - | 照合 |
| 105 | 解決 | ROSTER[0] L899-907 全100 | - | 照合 |
| 106 | 解決 | オーナー3択が全て実装: 自分で踏む L1895-1906/L1925-1929、カバー到着待ち（待つ間受け手が塁へ歩く）L2122-2131、挟殺版=自分が近ければ踏みに行く L2214-2223 | - | 照合 |
| 107 | 部分解決 | OI-092と同じ＋stepBall の DRAG 空気抵抗 | - | 照合 |
| 108 | 解決 | buildFielders L923-929 の能力→挙動写像は正しく配線（一塁手だけ上手くなる経路はない）。体感差は判定不能 | - | 照合 |
| 109 | 解決 | tagNearbyRunner L1815-1828＋毎フレーム適用 L1865-1867（塁間タッチ、押し出し例外 L1821 込み） | - | 照合 |
| 110 | 対象外 | 依頼→`sol_report_pattern_20260801.md` 取得済み。消化状況は§1 | - | - |
| 111 | 対象外 | 運用ルール化済み | - | - |
| 112 | 対象外 | 受領記録。消化状況は§1 Codex行 | - | - |
| 113 | 対象外 | 受領記録。消化状況は§1 P1〜P6 | - | - |
| 114 | 判定不能 | charterに分布測定記録（フライ安打率19%・HR/フライ比14%）はあるが本レビュー未検証 | 分布測定 | 照合 |
| 115 | 解決 | OI-039と同じ | - | 実行 |
| 116 | 解決 | diveReach L1176＋飛び込み判定 L2883-2903＋見た目 L2893-2903 | - | 照合 |
| 117 | 解決 | L2950-2953 `const pg=clamp(0.995 - 0.09*(1-(prim.cat||70)/100) - clamp((gsp-40)/55,0,1)*0.11*(...), 0.60, 0.995);`（速い打球ほど・捕球が低いほど弾く）＋deflectBall | - | 照合 |
| 118 | 解決 | L2976-2978 飛距離・初速の表示 | - | 照合 |
| 119 | 判定不能 | 較正往復の現在値（charter: 29%、実際24%）。本レビュー未再測定 | 実測 | 照合 |
| 120 | 部分解決 | OI-092と同じ | - | 照合 |
| 121 | 解決 | OI-039と同じ | - | 実行 |
| 122 | 解決 | 捕球・飛び込み判定はチーム非依存（stepFlight共通経路。CPU守備も同じ関数） | - | 照合 |
| 123 | 解決 | planPlay の先読み path はバウンド込み L1267-1276（stepBall実物理）＋着地後0.2秒毎の再照準 L2735-2748 | - | 照合 |
| 124 | 判定不能 | S-12 | - | 照合 |
| 125 | 判定不能 | 打球分布の統計測定が要る | 分布測定 | 照合 |
| 126 | 部分解決 | OI-092と同じ | - | 照合 |
| 127 | 部分解決 | 回り込みは実装済み: L886 `const ROUND_OFF = 0.28;`＋L1297-1302 `for(const M of [ROUND_OFF, 0]){...}`（余裕を持って入れる点を先に探す）。ただし OI-237 の担当フリッカー経路が残る（§0-2） | ギャップの二塁打 | 照合 |

### 第3部 2026-08-04（OI-128〜236）

| OI | 判定 | 根拠 | 再現条件 | 深さ |
|---|---|---|---|---|
| 128 | 解決 | judgeAtBase L2435-2441 `if(outMade && b>=2 && !T.relayed && ... && br.p<1 && T.fieldT<2.2){ ... T.target=1; }`（併殺転送） | - | 照合 |
| 129 | 解決 | L1848-1851 `const flyWait = (T.kind==='fly' && runners.some(...) && T.t<1.5);`（捕球後1.5秒はSを受け付ける） | - | 照合 |
| 130 | 対象外 | 設定作業。後の裁定で ROSTER[1] は通常値へ（L908-911 コメントに経緯） | - | 照合 |
| 131 | 対象外 | 設定作業（ROSTER[0] `弾:2`） | - | 照合 |
| 132 | 判定不能 | 見た目の合否はオーナー体感のみ | 実機目視 | - |
| 133 | 判定不能 | 「MLB The Show水準」は単一HTML・外部画像なし制約の天井が charter L139 に自認されつつ**オーナーに未伝達**。要相談 | - | 照合 |
| 134 | 対象外 | 進め方指示 | - | - |
| 135 | 判定不能 | 環境要因 | - | - |
| 136 | 解決 | L1675-1684（走者が実際に走るかで勝負判定）＋L1714 転送は外野限定 | - | 照合 |
| 137 | 部分解決 | 挟殺の各種決着は追加済み（セーフ L2258-2262・脱出 L2268-2276・時間切れ L2297-2304）。ただし §1 P4=挟殺決着時に本塁以外へ走行中の別走者はそのまま確定される | 挟殺中に別走者が二・三塁へ走る | 照合 |
| 138 | 解決 | tagPhase L3978-4007（ハーフウェイ）＋L4010 `if(back) live.forEach(r=>{ r.tagUp=false; });`（Xで取消し帰塁可） | - | 照合 |
| 139 | 解決 | REC実装 L98-145（F=直前/G=60プレー、0.1秒刻み） | - | 照合 |
| 140 | 未解決 | §0-1。beginThrowPhase L1770-1772 が捕球の瞬間に非押し出し走者を `r.origin` へ戻す。b0804-33 で実行再現済み | 0死(または1死)・走者二塁のみ・二塁/一塁方向のゴロ | 実行 |
| 141 | 未解決 | 仕様は L2691 `else if(r.origin===2) go = !'遊三投捕'.includes(pn);` に実装。**しかし捕球時に L1770-1772 が上書きし、「二塁方向のゴロは自動スタート」の実効が失われる**（スタート直後に戻る＝OI-238の見え方） | 同上 | 実行 |
| 142 | 判定不能 | 第9陣の関節化は実装（seg L3575-3604）。「不自然さ」の合否は体感 | 実機目視 | 照合 |
| 143 | 解決 | L1714-1716 `const isOF = Math.hypot(f.cx,f.cy) >= 158; const nb = (isOF && fallback.m < -0.6) ? ...` | - | 照合 |
| 144 | 部分解決 | OI-073と同じ（多層防御＋sweep128不変条件を実行PASSで確認。残存=12秒打ち切り・0.13閾値） | - | 実行 |
| 145 | 解決 | L1855 keyHeld＋L2495-2500 holdOpen（押している間は確定しない） | - | 照合 |
| 146 | 判定不能 | オーナー提案経路（codex画像→3Dモデル）は不採用のまま説明もされていない。要相談 | - | 照合 |
| 147 | 解決 | L2703-2713 固定刻み PHYS_H=1/240＋stepBall（マグヌス・バウンド） | - | 照合 |
| 148 | 解決 | markOut L2472-2473 `const beforeFirst = (r.origin===0 && r.p < 1-1e-9); if(beforeFirst) kind='force';`＋L2541-2543 第三アウトforce/flyで `runs=0` | 2死三塁の一塁きわどいアウト | 照合 |
| 149 | 解決 | L2808-2821（ファウルも飛行を見せて確定） | - | 照合 |
| 150 | 判定不能 | カメラ角の合否は目視 | 実機目視 | 照合 |
| 151 | 判定不能 | 再現録画なし。候補: L4019-4022 mustReturn中はSが効かない（正しい仕様）／tagPhase中のS=ハーフウェイ解釈（L3980-4006）が「押したのに進まない」に見える可能性 | S押下が無視される録画 | 照合 |
| 152 | 解決 | 1-3キー個別指定 L3967-3971 | - | 照合 |
| 153 | 解決 | L3356-3361 バント自動コンタクト（タイミング不要）＋方向はカーソル左右（UI文 L4120） | - | 照合 |
| 154 | 対象外 | 設問（回答済み: G=60プレー） | - | - |
| 155 | 解決 | 捕球発火を受け手基準に L2037-2042＋高さ判定 L2064-2070（頭上9.5ft超は通過） | - | 照合 |
| 156 | 対象外 | 設問（回答済み: F=直前1プレー） | - | - |
| 157 | 部分解決 | 牽制は L2351-2361（塁間なら挟殺、戻り遅れはタッチアウト）で「なぜかセーフ」の主経路は閉塞。ただし §1 P3 の10.8ft容認と、L3239 `runners.forEach(r=>{ if(r.jumped){ r.goal=r.origin; r.cmd=null; } });`（飛び出した走者は必ず戻る＝次塁強行の択がない）という単純化が残る | 牽制場面の実機 | 照合 |
| 158 | 判定不能 | 打球分布測定が要る | 分布測定 | 照合 |
| 159 | 解決 | chooseThrowTarget 全面書換＋中継の投げ直し L2116-2120 | - | 照合 |
| 160 | 解決 | L2810-2821（フェア打球と同じくカメラで追い、落ちてから確定） | - | 照合 |
| 161 | 解決 | judgeAtBase steal L2372-2382 `const goingOn = runners.find(r=>!r.out && r.p>b-0.02 && r.p<b+0.999 && r.goal>b+0.02); if(goingOn){ ...プレー続行 }` | - | 照合 |
| 162 | 解決 | L2659 `put(4, ['捕','投']);`＋rundownCover（本塁カバー） | - | 照合 |
| 163 | 解決 | L1173 `const BACKSTOP_R = 62;`＋バックネット実装 L538-＋L1135 送球が止まる | - | 照合 |
| 164 | 解決 | OI-148と同じ | - | 照合 |
| 165 | 解決 | OI-152と同じ | - | 照合 |
| 166 | 解決 | OI-155と同じ | - | 照合 |
| 167 | 解決 | L2258-2262 relay中セーフ判定 | - | 照合 |
| 168 | 解決 | L2207-2209 `R.trig = rnd(0.11, 0.30);`（切り返しの間合いを毎回変える。マスターは「記録なし」だが実装済み） | - | 照合 |
| 169 | 未解決 | 難易度は単一の DIFF（L938-946）のみ。打撃・投球・守備の3系統別選択は存在しない | - | 照合 |
| 170 | 部分解決 | σ再較正 L1463-1467。頻度の体感合否は未検証 | 実機プレー | 照合 |
| 171 | 未解決 | 送球がベンチ・スタンドへ入った時の2個進塁（ボールデッド）規則が存在しない。バックネットで物理的に止まる（L1135-1136）だけで進塁権は与えられない | 悪送球が場外方向へ大きく逸れる | 照合 |
| 172 | 判定不能 | L750「打席の視点では描かず」とあるが L3464-3466 は常時描画（`drawMesh(MESH.backstop, IDM);`、打席カメラz=-45はネットz=-62より内側という理屈）。飛球追跡カメラでの遮蔽は目視確認が要る | ファウル・レフト線の実機目視 | 照合 |
| 173 | 対象外 | 進め方指示 | - | - |
| 174 | 解決 | L2387 `if(b==='P') return concludeOrChaseHome();`＋L2336-2348。残存経路は§1 P1（12秒打ち切り） | - | 照合 |
| 175 | 部分解決 | 自動タッチアップは L3003-3008 `const deep = dist>225 && ball.t>3.0; const wants = (r.cmd==='X') ? false : ((r.cmd==='S') || r.tagUp || (deep && r.origin===3 && baseOuts()<2));`＝深いフライの三塁走者のみ。二塁走者のタッチアップ等は手動のみ | 深めの中飛で二塁走者 | 照合 |
| 176 | 解決 | L3222-3225（四球は走者を行き先に着かせ0.9秒で次へ） | - | 照合 |
| 177 | 判定不能 | OI-172と同じ | - | 照合 |
| 178 | 解決 | OI-159と同じ | - | 照合 |
| 179 | 解決 | OI-145と同じ | - | 照合 |
| 180 | 解決 | OI-174と同じ | - | 照合 |
| 181 | 判定不能 | OI-172と同じ（「非表示にしたはず」の対象特定記録がなく、現行は常時描画＋位置の理屈で回避） | - | 照合 |
| 182 | 対象外 | メタの問い→§0-3 | - | - |
| 183 | 解決 | L1791-1795 `flyBy: kind==='fly' ? f.n : null,`＋L2563-2566 表示は捕った野手で作る | - | 照合 |
| 184 | 解決 | ui() L4095-4099 E列＋finishPlay L3110-3111 集計 | - | 照合 |
| 185 | 解決 | L3485 `drawFigure(defKit, catcher.cx, catcher.cy, Math.PI, 0, 1.0, 0,...)`＝等倍 | - | 照合 |
| 186 | 部分解決 | エラー後の続行経路は L2447-2458（chase走者がいれば投げ直して続行）。ただし当該録画の真因記録がなく、同一場面の閉塞は確認できない | 該当場面の録画 | 照合 |
| 187 | 対象外 | 本レビューがその実行 | - | - |
| 188 | 部分解決 | charter未解決3のとおり: 封殺カバー・挟殺・送球先分布・フライ層は点検済み、中継・safeReach個別精査は未了。未実装概念多数（§1 Codex行） | - | 照合 |
| 189 | 判定不能 | 体感 | 実機目視 | - |
| 190 | 部分解決 | L2764-2767 `if(freed!=null) rundownCover(freed, plan2.f, prim);`（旧担当をカバーへ引き抜かない）は実装。ただし降格した旧担当は新しい指示を持たず、担当往復（フリッカー）も未対策（§0-2） | ギャップ・壁際の打球 | 照合 |
| 191 | 解決 | L2013-2035（受け手が実際に来る場所へ寄る）＋L2043-2056（頭上越え/着弾の瞬間に最寄りが回収） | - | 照合 |
| 192 | 解決 | OI-025と同じ（表示と打ち切りの分離は L2477-2481 コメントに明記） | - | 照合 |
| 193 | 解決 | OI-039と同じ | - | 実行 |
| 194 | 対象外 | 発注→charter が回答（補修方針） | - | - |
| 195 | 解決 | OI-174と同じ | - | 照合 |
| 196 | 解決 | OI-091と同じ | - | 照合 |
| 197 | 解決 | L1361-1369 `if(crossed || throughBase) r.v*=0.65;` | - | 照合 |
| 198 | 解決 | L2735-2748（ゆっくり転がる球は止まる地点へ全速直行・0.2秒毎再照準） | - | 照合 |
| 199 | 解決 | 平地化 L1526-1568（throwAngle 二分探索・目標を高さ5ftで通過）＋居残り判定 L2018-2033。test6 実行PASS | - | 実行 |
| 200 | 解決 | L1463-1467 throwOffset（送=acc）＋ROSTER 送列＋R画面 L4198 | - | 照合 |
| 201 | 解決 | L2775-2779 fairLocked＋deflectBall側の確定＋test8 実行PASS | - | 実行 |
| 202 | 解決 | L3978-3979 `const tagPhase = ... && baseOuts()<2;`＋test7 実行PASS | - | 実行 |
| 203 | 解決 | OI-136と同じ | - | 照合 |
| 204 | 解決 | L1547-1550 コメント＋実装（距離上限撤廃・届く限り高さ5ft通過）。test6 実行PASS | - | 実行 |
| 205 | 解決 | L1990-2002 バックアップ（送球線延長20ft後方） | - | 照合 |
| 206 | 対象外 | 旧ビルド録画と判明→ビルド刻印 L4287 `document.title += ' ['+BUILD+']';`＋録画JSONの`版` L135 | - | 照合 |
| 207 | 判定不能 | OI-151と同じ | - | 照合 |
| 208 | 解決 | OI-174と同じ（charter「現行コードに実在したバグ」→修理・単体再現記録あり） | - | 照合 |
| 209 | 対象外 | メタの問い→§0-3 | - | - |
| 210 | 対象外 | 設問→ビルド刻印で解決 | - | - |
| 211 | 判定不能 | 環境要因 | - | - |
| 212 | 解決 | L1459-1461 コメント（0.028→再較正の経緯）＋L1466 `const sigma = d*(0.004 + 0.016*(100-acc)/100 + 0.012*run);` | - | 照合 |
| 213 | 解決 | L3319-3329（プレー確定後も速度を持つ球は物理継続、投手のそばで手に収める） | - | 照合 |
| 214 | 解決 | L1162-1170 線形摩擦 `const dec=(5 + 0.055*sp)*dt;` | - | 照合 |
| 215 | 解決 | 見た目実装 L2893-2903（diveT/jumpT）＋ゴロ外縁 L2947-2949 | - | 照合 |
| 216 | 部分解決 | 膝・肘の2セグメント化 L3575-3604。首・手首は未分離（charter自認） | - | 照合 |
| 217 | 判定不能 | ランタイム生成テクスチャ（buildGroundTex L373-）が制約下の天井である旨がオーナーに未伝達（charter L139）。要相談 | - | 照合 |
| 218 | 解決 | OI-155と同じ（6.5ftずれの本塁ロブ捕球を機械確認した記録あり） | - | 照合 |
| 219 | 解決 | L3992-3999（打者走者はフライ中もSで先の塁へ。上限は塁単位 L3995-3997） | - | 照合 |
| 220 | 部分解決 | 当該修正は適用条件付きで実装済み L1709-1716（コメント`★外野手（本塁から158ft超）だけに適用する`＋`const isOF = ... >= 158;`）。**しかし「場面パッチには必ず適用条件を付ける」規律は仕組み化されておらず、beginThrowPhase の走者リセット（L1770-1772）が適用条件（走者が既に踏み切っているか）を持たない同型の現物として残っている＝OI-238** | §0-1 | 実行 |
| 221 | 解決 | OI-214と同じ（強95mph≈283ft等の実測記録は charter） | - | 照合 |
| 222 | 部分解決 | 前進守備 L3090-3093 `const inField = (S && S.bases && S.bases[2] && baseOuts()<2) ? 0.78 : 1.0;`＋カットオフ L1981-1988。学習概念のうち実装は2つのみ | - | 照合 |
| 223 | 解決 | L1153-1155 縦反発 0.55（横維持0.78の据え置き理由もコメントに記録） | - | 照合 |
| 224 | 解決 | buildOval L788-（等幅楕円）＋当たり判定 L1076 `Math.hypot(dx*0.60, dy*1.30)`（L787 コメント「縦横比2.17:1は当たり判定と一致」） | - | 照合 |
| 225 | 解決 | OI-224と同じ | - | 照合 |
| 226 | 解決 | endRundown L2318-2322 `if(T.rd && T.rd.sub==='relay'){ T.receiver=T.rd.recv; ... T.relayTo=4; ...}`（空中の球は受け手に捕らせてから本塁転送） | - | 照合 |
| 227 | 解決 | L1762-1780 追い越し禁止＋test11 実行PASS | - | 実行 |
| 228 | 解決 | 挟殺中も操作可能: updateRunnerCommands は throwing 相で有効 L3951、setAutoGoal L4234 `if(!force && r.cmd) return;`＝S/X指示は挟殺の自動指示に上書きされない | - | 照合 |
| 229 | 判定不能 | 挟殺の悪送球率は L2230 `R.fumble = Math.random() < A.fumble(recv.cat)*2.2;`（通常の2.2倍）。「簡単に逃れられる」かは頻度実測が要る | 挟殺20回の実測 | 照合 |
| 230 | 解決 | L2264-2276（逃走球で挟殺崩壊→通常回収へ移行、打ち切らない） | - | 照合 |
| 231 | 解決 | L2904-2907 `ball.x=prim.cx; ... ball.vx=ball.vy=ball.vz=0;`（捕った球をグラブへ固定） | - | 照合 |
| 232 | 部分解決 | 当該場面（一二塁間強襲の逆転）は L2658 で対処。ただし「カバーか追球か」の一般判断は打った瞬間の planPlay 一発＋0.35秒毎の再割当で、フリッカー未対策（§0-2）。OI-237 が同系統の再発疑い | ギャップの打球 | 照合 |
| 233 | 解決 | L2074-2078（捕球スナップで速度0）＋test9 実行PASS | - | 実行 |
| 234 | 解決 | throwFlatOK L1570-1584＋中継条件 L1955 `if(far>maxD || !throwFlatOK(far, throwSpeed(armEff(T.thrower), far))){`＋test10 実行PASS | - | 実行 |
| 235 | 解決 | OI-234と同じ | - | 実行 |
| 236 | 対象外 | メタの問い。実質の答え=OI-222（学習概念のうち実装は前進守備・カットオフの2つ） | - | - |

### 第4部 2026-08-05（OI-237〜239）

| OI | 判定 | 根拠 | 再現条件 | 深さ |
|---|---|---|---|---|
| 237 | 判定不能 | §0-2。候補経路3つを行番号つきで特定（担当再割当フリッカー L2753-2769／壁当て再計画 L2843-2849／飛び込みスタン L2893-2895）。録画が無いため確定できない | G録画でツーベース時の `fld` 列 `*`（担当マーク）の移りを見る | 照合 |
| 238 | 未解決 | §0-1。beginThrowPhase L1770-1772 の infield 分岐が捕球の瞬間に非押し出し走者を元の塁へ戻す。b0804-33 のライブ実行で goal 3→2 の反転を再現（transfer 開始フレーム・オーナー録画と一致） | 0死・走者二塁・二塁方向のゴロ | 実行 |
| 239 | 対象外 | 本レビューがその実行 | - | - |

---
## 3. 回帰テストの検出力（発注の問い4への回答・実行で確認）

手順と結果（すべてブラウザ内・ファイル不変更・終了後リロードで復元）:

1. **現行 b0804-33 でハーネス全文を実行 → 13本＋sweep128 全PASS**。CC の申告「11/11＋sweep128 全PASS」は正しい（テスト名で数えると test1 が2本あるため13表示）。
2. **同じビルド・同じセッションで OI-238 が1発で再現**（0死・二塁・二塁方向ゴロ→捕球の瞬間に goal 3→2）。**「全PASS」と「オーナーが遊べば即バグに当たる」が同時に成り立つ**ことを実測で示した。
3. **変異テスト**: `concludeOrChaseHome` を `concludePlay` 直行へ差し替え（＝S-7「本塁未到達で得点打ち切り」を意図的に再導入。オーナーが4回踏んだ 8/4 修理箇所の逆戻し）。この状態でハーネス全文を再実行した結果——**13本＋sweep128 が全PASSのまま**。つまり**現行ハーネスは、オーナーが最も多く踏んだ得点バグの再発を1件も検出できない**。
   - 理由: sweep128 の走者保存則 `startBases+1 == endBases+runsGained+outsGained` は、走者が「早すぎる得点」になっても run として数えるため保存則が破れない。個別テストにも本塁到達と得点計上の順序を見るものが無い。
4. **fixture腐敗の残存リスク**（照合）: test4 は「バウンドが発生した試行数」を出力せず（`_test_harness_20260804.js` L131-140、`bounced` はループ内ローカル）、test9 は `plays`（15試行中プレー状態に達した数）を出すが合格条件に使っていない（L232-236）。守備・物理の較正が進んで**意図した場面が発生しなくなっても両者は PASS を出し続ける**。8/5未明に修理された3件と同じ型の腐敗が、今後は「偽PASS」側で起きる構造が残っている。
5. **提案（判定の帰結として）**: (a) 各テストに「場面が成立した回数」の下限を合格条件へ入れる（test3 が既にやっている形が正しい）。 (b) OI-238 用の回帰テスト（0死二塁・二塁ゴロ→捕球後 0.3 秒時点で二塁走者の goal が 3 のままであること）を追加する。 (c) S-7 用に「本塁 p<3.97 の走者がいる状態で runs が増えないこと」の不変条件を sweep へ足す。

## 4. オーナーが与えた仕様のうち実装で無視・毀損されているもの（発注の問い5への回答）

| 仕様 | 状態 |
|---|---|
| OI-141 正面ゴロはスタートせず・二塁/一塁方向や難しい打球は自動スタート | **実装済みだが毀損**。打った瞬間の判定（L2681-2695）は仕様どおり。捕球の瞬間に L1770-1772 が仕様の判断ごと破棄する（§0-1）。オーナーから見れば「仕様が無視されている」のと同じ見え方 |
| OI-106 カバーが間に合わない時の3択 | 実装済み（L1895-1906・L2122-2131・L2214-2223） |
| OI-098 塁間の走者には持ったまま近づく | 実装済み（L1741-1759・L2084-2103）。過剰適用の抑制条件も付与（L1746-1752） |
| OI-168 挟殺の投球タイミングを毎回変える | 実装済み（L2207-2209 `R.trig=rnd(0.11,0.30)`）。指摘マスターの「記録なし」は追跡漏れで、実装は存在する |
| OI-056 守備の思考を走塁の思考から独立させる | 部分毀損が残る。goal の直接参照は排除したが、goal から毎フレーム導出される dir（L1356）を介して意図が同フレームで漏れる（§1 P5） |
| OI-169 難易度3系統 | 未実装 |
| OI-171 送球のスタンドイン規則 | 未実装 |
| OI-133/146/217 見た目の到達水準（The Show級・写真級） | 単一HTML制約の天井が charter に自認されつつ**オーナーに未伝達のまま**。仕様の握り直しが必要（実装では解決しない） |

## 5. この手法では原理的に見つけられない種類の誤り（深さの申告）

1. **体感・頻度の不一致**: 「肩が強すぎる」「送球エラーが多すぎる」「バウンドしている感じがない」等の較正の合否は、コードを読んでも決められない。統計測定（分布・率）と、最終的にはオーナーの体感でしか閉じない。本レビューの「解決」は「指摘された機構がコード上で塞がれている」ことの確認であって、「オーナーがもう不満を持たない」ことの保証ではない。
2. **見た目・カメラの合否**: オブジェクトの遮蔽（OI-172系）・モーションの自然さは目視でしか判定できない。
3. **書かれていない場面の相互作用**: 本レビューは指摘239件を起点にした照合が主で、指摘に現れていない状態組合せ（例: 挟殺中の盗塁指示、四球と盗塁の同フレーム衝突など）の網羅探索はしていない。sweep128 も打球種×塁2種に限られる。
4. **較正の揺り戻し（退行の一種)**: 定数の時系列を追っていないため、「以前オーナーが合格を出した値から黙って動いた」ことは検出できない。基準値が測定条件つきで固定されていない項目（S-12）はこの型に無防備。
5. **乱数依存の稀な経路**: 実行確認は代表条件の単発〜数十試行で、確率的にしか出ない経路（送球ずれの裾・fumble 連鎖）は踏めていない可能性がある。
6. **自分の見落とし**: 4,291行の全読みを行ったが、1回の照合で全分岐の相互作用を保持することはできない。特に「解決」判定の中に、別経路からの回り込みを見逃したものが混ざりうる（OI-238 がまさに「主経路は直したが別の書き手が残っていた」型である以上、同型が他にもある前提で扱うべき）。

## 6. 検証環境の記録

- 実行: ローカルHTTPサーバ（`python -m http.server 8043`）＋ブラウザで `baseball3d.html` [b0804-33] を読み込み、コンソール相当のJS実行で (1)OI-238再現ループ (2)ハーネス全文 (3)変異ハーネス を実施。変異はメモリ内のみで、終了後にページをリロードして破棄した。`baseball3d.html`・`_test_harness_20260804.js` への書き込みは一切していない。
- 本ファイル以外に生成・変更したファイルは無い。

---

## 7. 追補（同日）: OI-240 の真因と、OI-190/237/240 の機構統一（実行で確認）

> 追加録画 `野球ゲーム記録_20260805_110336.json`（版 b0804-33、0死・走者なし・91mph -10° 方向-27°）「サードのほうが近かったのにショートがゴロをおいかけたせいでひっとになった」を受けての追加判定。録画の機械読解と、b0804-33 での**ライブ再現**の両方を行った。

### 7-1. 録画とライブ再現の一致（実行）

録画の事実（自分でJSONを読み直して確認）: t=0.3 で打球担当が 三→遊 に交代（その瞬間 三→球 52.3ft / 遊→球 102.7ft）。打球は t=0.8 に三塁手から 9.1ft を通過。交代後の三塁手は役割タグ無しのまま (-52,82)→(-24,41) へ**打球と逆方向に歩き続けた**。

ライブ再現（同条件を b0804-33 で実行）:
- **t=0.37**（交代ゲート 0.35 秒を越えた最初の判定機会）に 三→遊 へ交代。その瞬間 三→球 56.6ft / 遊→球 106.7ft ＝**近い方が外された**。
- 交代後の三塁手: `coverBase=null`（無役）のまま、目標が **(-10.7, 22.4)** に凍結。この点は打球が t≈0.1 に通過済みの地点＝**3秒前にボールが居た場所へ歩き続ける**。t=0.8 に打球が 15.4ft 横を通過しても反応しない。録画の「逆方向に歩く三塁手」と同じ絵。

### 7-2. コード上の真因（3点、逐語）

**(a) 担当選び直しに「既に追っている野手への慣性」が存在しない。**
`stepFlight` L2753-2769（着地後0.35秒ごと）が `planPlay(ball, true)` を呼び、`planPlay` L1312 は
```js
if(hit.t<best.t) best={f, t:hit.t, x:hit.p.x, y:hit.p.y, ...};
```
＝**純粋な最小時間比較**。現担当への加点・引き留めは一切ない。僅差でも毎回入れ替わる。

**(b) 選出の物差しは「現在の距離」ではなく「打球の将来の通り道への到達時間」だが、その到達時間の計算が現担当に不利に歪んでいる。**
L1297-1299:
```js
for(const p of pool){
  if(react+runTime(f.sp,dOf(p))+M <= p.t){ hit={t:p.t,p}; break; }
}
```
- `react`（L1262 `F_REACT=0.28` 基準）は**選び直しのたびに再徴収**される。既に反応済み・走行中の野手にも 0.28 秒を課す。
- `runTime` L889-892 は**静止からの加速**を仮定し、`f.v`（現在速度）を見ない。
この2つにより、球のすぐ近くを 0.4〜0.6 秒後に通過する点は「近い野手でも間に合わない」と判定され、球足が落ちる遠い将来点に間に合う**遠い野手が勝つ**。本件はまさにこの型: 三塁手は近い将来点を全て「不可」とされ、fallback（L1303-1311）の遠い評価値になり、減速後の球を拾える遊撃手に負けた。オーナーの物差し（今の距離）でも実физическая可達性（9〜15ft 横を通過・グラブ+飛びつき圏 8.4ft と際どい）でも三塁手が優位なのに、モデルの物差しだけが逆を出した。

**(c) 外された旧担当には役割も新しい目標も与えられない。**
L2757-2767:
```js
if(plan2.f !== prim){
  const freed = plan2.f.coverBase;
  ball.primary = plan2.f; ...
  fielders.forEach(f=>{ f.primary=(f===plan2.f); });
  plan2.f.coverBase = null;
  setTarget(plan2.f, plan2.x, plan2.y);
  if(freed!=null) rundownCover(freed, plan2.f, prim);
```
新担当には `setTarget` があるが、**旧担当（prim）へのsetTarget・役割割当はどこにもない**。しかも OI-190 の修正で `rundownCover(..., prim)` から**明示的に除外**されている。旧担当は「直前まで担当だった時の目標」（本件では既に球が通過した昔のチャージ点）へ 0.88 倍速で歩き続ける（moveFielders L1408-1413）。再照準（L2735-2748）は `prim` にしか働かないので、この凍結目標は二度と更新されない。

### 7-3. OI-190 / OI-237 / OI-240 は同一機構か → **同一（1つの縫い目の3つの症状）**

縫い目＝**担当再割当（L2753-2769、壁当て版 L2843-2849）が「選び直し」だけを行い、外された側の後始末を持たない**こと。

| 症状 | 何が見えたか | 縫い目のどの面か |
|---|---|---|
| OI-190（8/4） | 捕れそうな三塁手が急に三塁カバーへ走った | 外された旧担当が rundownCover に拾われて塁へ走った |
| OI-237（本日・右翼） | 追跡中に一瞬止まってまた追った | 外された旧担当が凍結目標に到達して停止→0.35秒後の再選出で復帰 |
| OI-240（本日・三遊間） | 近い三塁手が外され、無役のまま逆方向へ歩き、ヒットになった | (a)慣性なし＋(b)歪んだ物差しで近い方を外し、(c)無役で放置 |

**OI-190 の 8/4 修正は症状1だけを塞いだ**（旧担当をカバーに使わない）。その結果、外された野手の行き先が「塁カバー」から「**無役で凍結目標へ歩く**」に変わっただけで、縫い目そのもの（選び直しの慣性欠如＋後始末欠如）は開いたまま。これは §0-3 の機構1（片側だけの修正を、もう片側が黙って打ち消す/別の形で再出現する）の第2の実例であり、**オーナーの「直したはずが再発する」を b0804-33 上で実証できた2件目**（1件目=OI-238）。charter「再確認リスト」の「担当交代時に旧担当をカバーへ引き抜かない」という記録は、この縫い目全体を塞いだかのように読めるが、実際は3症状のうち1つを別の症状に変換したにすぎない。

- 判定の帰結（実装は別担当）: (i) planPlay の比較に現担当ボーナス（例: hit.t への -0.3〜-0.5 秒）を入れる (ii) 選び直し時の react を「未反応の野手のみ」に課し、runTime に現在速度を渡す (iii) 外された旧担当に必ず次の役割（最寄りカバー or 打球線上のバックアップ）と新しい目標を与える (iv) 回帰テストに「担当交代時、旧担当の目標が球の進行方向と 90 度以上ずれたまま放置されない」を追加。

### 7-4. OI-240 判定行（§2 の表への追加）

| OI | 判定 | 根拠 | 再現条件 | 深さ |
|---|---|---|---|---|
| 240 | 未解決 | §7-2。担当再割当 L2753-2769＝慣性なし（L1312）・再徴収される react と静止仮定の runTime（L1297-1299/L889-892）・外された旧担当への役割/目標の未割当（L2757-2767）。録画とライブ再現の両方で確認 | 0死・三遊間の強めのゴロ（91mph -10° spray-27）で t≈0.35s の担当交代を観察 | 実行 |

これに伴う集計の更新: **総件数 240、解決 149・部分解決 30・未解決 6・退行 0・判定不能 30・対象外 25**。§0-4 の表は本節の値が正（OI-240 追加後）。また §0-2（OI-237）の「判定不能」は維持するが、機構としては本節で OI-240 と同一の縫い目に統一されたため、縫い目を塞げば OI-237 も同時に消える見込みが高い。

---

## 8. 追補2（同日）: OI-241「無駄な回り込みでヒットが増える」の判定

> 追加録画 `野球ゲーム記録_20260805_120407.json`（版 b0804-33、6プレー）。プレー3〜5の座標列を自分で読み直して確認した（担当交代の時刻・距離は指摘どおり。さらに下記の新事実を追加で確認）。

### 8-1. 欠陥は2つ＋増幅器1つ（すべて行番号つき）

**(a) 回り道（プレー5、担当交代なし）— 真因は「チャージ条件の適用条件が回っていない」**

再照準（追跡中の目標決め）は `interceptPoint` L1180-1201。第5陣で入れたチャージ条件は:
```js
const spd=Math.hypot(b.vx,b.vy);
const margin = spd>70 ? ROUND_OFF : 0.06;
if(reactOf(f)*0.4+runTime(f.sp,d)+margin <= t) return [b.x,b.y];
```
（L1194-1196）。**「転がる打球には突っ込む」の判定が球速70ft/s（≈48mph）を境にしている**ため、ごく普通の内野ゴロ（70〜90mph＝103〜132ft/s、転がり減速は約10ft/s²）は**最初の2〜3秒間ずっと「強襲=0.28秒の余裕を待つ」側に分類される**。プレー5（72mph）では、三塁手が本塁から88ft地点で捕れたはずの打球の通過時球速が約90ft/s>70のため、余裕0.28秒を満たす点が見つかるまで候補が深い方へずれ続け、0.2秒ごとの再照準（L2747 `const ip=interceptPoint(prim); setTarget(prim, ip[0], ip[1]);`）がそのたび「少し先」を出す＝**打球の外野側を平行に走る**録画の絵になる。135ft地点まで見送って内野安打。
これは **OI-127 の修正（回り込み余裕）と第5陣のチャージ修正の合成の穴**: チャージ条件は入っているが、その適用条件（球速70ft/s）が「エラー後の死んだ球」を想定した値のままで、**普通のゴロがほぼ永遠に対象外**。OI-220 の規律で言えば「適用条件は付いているが、条件の物差しが場面に合っていない」型。

**(b) 担当交代（プレー3・4）— OI-240 と同一の縫い目（§7）**

録画で確認: プレー3は t=0.7、プレー4は t=1.0 に、**打球へ47〜60ftまで詰めていた二塁手から、179〜201ft後方の右翼手へ担当が移る**。機構は §7-2 と同一——選定側 `planPlay` は L1297 `if(react+runTime(f.sp,dOf(p))+M <= p.t)` で **毎回 react を再徴収し・静止からの加速を仮定し・第1パスは常に M=ROUND_OFF** のため、球を追走中（球速>野手速）の現担当は「どの将来点にも間に合わない」と判定され、減速後の球を正面から拾える遠い野手が勝つ。
**さらに悪いことに、選定側(planPlay)と照準側(interceptPoint)で物差しが食い違っている**: 照準側は第5陣で `react*0.4`＋球速条件付き margin へ直したが、選定側は旧来の**フル react＋無条件 ROUND_OFF 第1パス**のまま（L1291 `const react = reactOf(f);` / L1297）。つまり**「担当がどこへ向かうか」を直した修正が「誰を担当にするか」には適用されておらず、チャージ中の野手を選定側が『間に合わない』と外す**。これが質問2への答え: 2つの計算は同じ再計算カスケード（stepFlight L2735-2748 照準 / L2753-2769 選定）に同居し、同じ到達可能性を**別々の式**で計算している。片方だけを較正するたび、もう片方との不整合が新しい珍プレーとして現れる——「直したはずが再発する」の直接の構造説明。

**(c) 増幅器 — 捕球判定は ball.primary にしか働かない**

プレー4の録画で新たに確認した事実: 担当を外された二塁手は t=1.8〜4.2 の間、**転がる球の1〜11ft横を並走し続けた**（t=3.9 で d=1ft）のに球を拾えず、右翼手が199ft地点で処理した。理由はコード上明確で、ゴロ捕球判定 L2937 は
```js
const grounded = ball.landed && Math.hypot(d2,gz) < CATCH_R+diveReach(prim)*0.5
```
と **`prim`（=ball.primary、L2725）に対してのみ**評価される。空中捕球判定（L2854-2883）も同じ。**担当でない野手は球の真横に立っていても物理的に捕れない**。だから (a)(b) の誤選定・誤照準は「近くの野手が拾って自然回復」せず、必ずヒット・進塁として表面化する。守備が「無駄に回り込んでいる」ようにオーナーに見える場面の少なくない部分は、この増幅器が誤選定を隠せなくしたもの。

### 8-2. (a) を直しつつ OI-127 を再発させない適用条件の案（1つ・実装は別担当）

margin の切替を「球速の絶対値」でなく**「球が自分に近づいているか」**で行う:
```
closing = (前回照準時より 野手→球 の距離が減っている)  ※ or dot(ballV, ball→fielder) > 0
margin  = closing ? 0.06 : ROUND_OFF
```
- プレー3/4/5型（球が野手へ向かって転がってくる／横を通る）: closing=真 → 突っ込む → 88ft・136ft地点で処理できる。
- OI-127 の原型（遠ざかる球を後ろから追って直線で行くと後逸）: closing=偽 → 回り込み余裕 0.28 を維持 → 再発しない。
- 併せて選定側(planPlay)も同じ margin 規則と「現在速度込みの runTime・反応済み野手の react 免除」を使い、照準側と物差しを一本化すること（(b) の恒久対策と同一）。回帰確認は test5（封殺アウト率）＋「三遊間72mph・一二塁間71mphで捕球地点が本塁から140ft以内」の新テストの2本立てが要る。

### 8-3. OI-241 判定行（§2 の表への追加）と集計の最終値

| OI | 判定 | 根拠 | 再現条件 | 深さ |
|---|---|---|---|---|
| 241 | 未解決 | §8-1。(a)=interceptPoint L1194-1196 のチャージ適用条件（球速70ft/s）が普通のゴロに回らず深い待ち点へ誘導 (b)=planPlay L1291/L1297 の旧物差し（フルreact・静止仮定・無条件ROUND_OFF）が追走中の担当を外す＝OI-240と同一縫い目 (c)=捕球判定が ball.primary 限定（L2725/L2854/L2937）のため誤選定が自然回復しない。録画6プレー中3プレーで実害（+75〜83ft・+1.3〜2.5秒、ヒット2・挟殺失点1） | 71-80mph・方向20°台または-42°のゴロ | 実行 |

**全指摘の最終集計（OI-001〜241、241件）**: 解決 149・部分解決 30・**未解決 7**（OI-140/141/169/171/238/240/241）・退行 0・判定不能 30・対象外 25。§0-4 と §7-4 の集計はこの値で置き換える。

**回帰機構の最終形（§0-3 の更新）**: 「直したはずが再発する」は3層で起きている——
1. **同じ量への複数の書き手**（走者goal: OI-238、担当と目標: OI-240/241）に適用条件のない上書きが残る。
2. **同じ物理量（到達可能性）の並行実装**（planPlay と interceptPoint）の片方だけを較正するたび、もう片方との不整合が新しい形の珍プレーとして再出現する（OI-241(b) が現物）。
3. **回帰テストが「直した経路」しか固定しない**うえ（§3 変異テストで実証）、(c) の増幅器（primary限定の捕球）のせいで選定・照準の誤りは必ずオーナーの目に見える形で出る。
