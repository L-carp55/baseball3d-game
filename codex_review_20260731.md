# baseball3d.html 最終コードレビュー

## 確認対象と読み取り条件

- 対象: `野球ゲーム/baseball3d.html`
- 最終確認スナップショット: 1924行、SHA-256 `AA86D24C69EB03BB8F8EB80460601C26608453569245FC6AB830A796658DC80D`
- 依頼文の「1847行」とは一致しない。途中で対象ファイルが更新されたため、古い読み取り結果は採用せず、最後に安定して確認できた上記スナップショットだけを根拠にした。
- コードはUTF-8で読み、JavaScript部分はNode.jsの構文確認を通過した。実際のブラウザで9イニングを再生した結果ではなく、実コードからの最終レビューである。
- 「確定」は、実行結果を推測せず、コードの条件と状態更新から直接確認できるもの。「未検証」は、実プレーをこのレビューで再現していないもの。

## 1. 野球ルールの正しさ

### 1-1. 強制判定が打者走者を確認していない（確定）

- 行: 765-771, 797, 984-994
- 該当コード:

  ```js
  function isForced(r){
    for(let b=1;b<r.origin;b++) if(!runners.some(x=>x.origin===b && !x.out)) return false;
    return true;
  }
  ```

  ```js
  else auto = (r.origin===0 || isForced(r)) ? Math.min(4, r.origin+1) : r.origin;
  ```

  ```js
  if(cand && runnerETA(cand,b)>0.02){
    if(!isForced(cand) && cand.origin>0) return beginRundown(cand, b);
    cand.out=true; cand.outKind='force'; outMade=true; cand.goal=cand.p;
  }
  ```

- 何が起きるか: 現在の`judgeAtBase`は、強制されていない走者を無条件に`force`にする処理を改め、ランダウンへ回している。この点は問題ない。一方、`isForced`のループは`b=1`から始まり、打者走者を表す`origin=0`を確認していない。
- なぜ問題か: 打者走者が一塁へ進む前にアウトになった後は、一塁走者・二塁走者への強制進塁が消える。ところが一塁走者は常に、二塁走者は打者走者のアウト後も、打者走者がまだ生きている前提で`isForced`が真になる場合がある。塁を踏むだけの封殺と、走者へタッチが必要な状態の区別を誤る。
- 具体的な修正: 打者走者を含むすべての前の塁を確認する。既存のコメントどおりにするなら、ループ開始値を`0`に変える。

  ```js
  for(let b=0;b<r.origin;b++)
    if(!runners.some(x=>x.origin===b && !x.out)) return false;
  ```

- 重大度: high

### 1-2. 三つ目のアウトではない強制アウトがあるだけで得点を消す（確定）

- 行: 1011-1025
- 該当コード:

  ```js
  const outs=countOuts();
  let runs=0; const nb=[null,null,null];
  runners.forEach(r=>{
    if(r.out) return;
    const g=Math.round(r.goal);
    if(g>=4) runs++;
    else if(g>=1) nb[g-1]={id:Math.random(), sp:r.sp};
  });
  if(S.outs+outs>=3 && runners.some(r=>r.out && (r.outKind==='force'||r.outKind==='fly'))) runs=0;
  ```

- 何が起きるか: `finishPlay`側の一律消去はなくなったが、`concludePlay`の`some(...)`は「このプレーで強制アウトまたはフライ捕球が1つでもあったか」しか見ていない。三つ目のアウトが何だったかは確認していない。
- なぜ問題か: 例えば同じプレーで先に強制アウトが1つあり、最後のアウトがランダウンの`tag`だった場合、ルール上は最後のタッチアウトが三つ目なら、先に生還した走者の得点を直ちに「強制アウトだから」という理由で消せない。現在のコードは先行する`force`だけを理由に`runs=0`にする。
- 具体的な修正: アウトを発生順に記録し、`S.outs+outs===3`となるアウトの種別だけを使う。三つ目が`force`または打者走者の一塁到達前アウト（フライ捕球を含む）の場合だけ得点を無効にする。単に`some`で全アウトを検索しない。
- 重大度: high

### 1-3. フェンスの三分岐は問題なし

- 行: 1146-1168
- `ball.z>9.4`かつ未着地なら`resolveHomer()`、着地済みなら`resolveHit(...,true)`、高さ9.4以下なら速度成分を反転して`wallHit`を設定している。したがって、コード上は「直接越える＝本塁打」「先に地面に触れてから越える＝エンタイトルツーベース」「高さ不足で壁に当たる＝壁で反射後の処理」という三分岐になっている。
- 結論: 問題なし（このレビューでは、実際の打球を全パターン再生したことまでは未検証）。

### 1-4. ランダウンの塁到達とタッチの判定順は問題なし

- 行: 890-912
- 該当コード:

  ```js
  if(R.t>0.25 && Math.abs(r.p-r.goal)<0.02 && Math.abs(r.goal-Math.round(r.goal))<0.01)
    return endRundown('セーフ！','#3fd66a');
  if(Math.hypot(R.holder.cx-rp.x, R.holder.cy-rp.z) < 3.0){
    r.out=true; r.outKind='tag'; r.goal=r.p; return endRundown('タッチアウト！','#95a3b4');
  }
  ```

- 何が起きるか: ランダウン中は、まず目標塁への到達を確認し、到達していなければ守備者との距離3.0ft以内をタッチアウトとして判定する。逃げ場が狭くなった場合も`outKind='tag'`として扱っている。
- なぜ問題か: 現在のコードでは、塁に到達した走者が同じ更新で守備者に近かった場合に、判定順だけでタッチアウトへ反転する問題は確認できない。
- 具体的な修正: この項目については不要。今後変更する場合も、塁到達の確認をタッチ距離の確認より後ろへ移さない。
- 重大度: なし

### 1-5. 犠牲フライの基本分岐は問題なし

- 行: 1202-1227
- `resolveCatch`は打者走者をアウトにし、三塁走者が離塁していなければ、深いフライ（距離225ft超かつ3.0秒超）で本塁へ向かわせる。二死では自動の犠牲フライを作らない。捕球後の送球は`beginThrowPhase(...,'fly',...)`へ渡している。
- 結論: 問題なし（ただし、1-2の三つ目のアウト時の得点処理は、この分岐にも影響する）。

### 1-6. ダブルプレーの基本経路は問題なし。ただし1-1の影響を受ける

- 行: 996-1009
- 一つ目のアウト後、`b>=2`、打者走者が一塁前に残っている、捕球が速い、という条件で`T.target=1`へ中継し、二つ目のアウトを判定する経路は存在する。
- 結論: 問題なし（強制されていない走者まで`outKind='force'`にする問題を直さない限り、非強制走者を含むケースは正しくない）。

### 1-7. 満塁の押し出し四球は問題なし

- 行: 1350-1384
- `forcedWalk`は、一塁・二塁・三塁がすべて埋まっているときだけ三塁走者を得点させ、各走者を一つずつ進め、打者走者を一塁に置く。四球の処理も`applyRunnerResult`、得点加算、塁更新の順で行っている。
- 結論: 問題なし。


## 2. 表示された動きと判定の一致

### 2-1. 先読みと実際の飛行で、1回の計算刻みが一致していない（確定）

- 行: 640-644, 646-653, 1117-1123
- 該当コード:

  ```js
  function planPlay(src){
    ...
    const h=1/240;
    for(let i=0;i<1700;i++){
      stepBall(b,h); t+=h;
    ...
  }
  ```

  ```js
  function updateFlight(dt){
    const sub=4, h=dt/sub;
    for(let i=0;i<sub;i++){
      stepBall(ball,h); ball.t+=h;
    ...
  }
  ```

- 何が起きるか: 先読みは常に1/240秒刻みだが、実際の飛行は画面更新の`dt`を4分割している。実際の画面更新が毎秒60回なら偶然同じになるが、毎秒30回なら1/120秒、毎秒120回なら1/480秒になる。`stepBall`には速度減衰とバウンドがあり、刻みが変わると計算結果も変わる。
- なぜ問題か: 画面上で実際に動いた打球と、野手を先回りさせるための予測経路が同じにならない。野手の到達判断が予測値を使うため、表示された打球位置と守備判定のずれを再発させる条件が残っている。
- 具体的な修正: 飛行を常に1/240秒の固定刻みで進める累積時間方式にして、実際の移動と`planPlay`が同じ`stepBall`呼び出し列を使う。少なくとも予測側だけ固定刻みにするなら、「通常の60fpsで一致する」という前提を明記し、別の更新間隔でも同じかを確認する。
- 重大度: medium

### 2-2. フェンスに当たって担当野手が変わったフレームは、前の野手で回収判定する（確定）

- 行: 1117-1129, 1155-1168, 1173-1181
- 該当コード:

  ```js
  const prim=ball.primary;
  ...
  if(ball.z>9.4){
    return finishPlay(...);
  }
  ...
  const plan=planPlay(ball);
  ball.primary=plan.f;
  ball.planT=ball.t+plan.t;
  fielders.forEach(f=>{
    f.primary=(f===plan.f);
    if(f.primary) setTarget(f, plan.x, plan.y);
  });
  ...
  const d2=Math.hypot(ball.x-prim.cx, ball.y-prim.cy);
  ```

- 何が起きるか: `prim`は壁への反射と新しい先読みを行う前に保存される。その同じ更新で新しい`ball.primary`へ交換しても、その後の`d2`、地面打球の回収判定、捕球判定の基準は古い`prim`を使い続ける。
- なぜ問題か: 画面では壁反射後の新しい担当野手へ切り替わっているのに、同じフレームの回収判定だけは前の野手の位置を基準にする。さらに担当変更時に`ball.dPrev`もリセットしていないため、古い野手との距離履歴が新しい野手の「近づいているか」判定へ持ち越される。
- 具体的な修正: 壁反射後に`prim=ball.primary`を取り直し、`dPrev`を未設定に戻す。壁反射の処理と、その後の距離・捕球判定を別の処理単位に分けるのが安全。
- 重大度: medium

### 2-3. 捕球判定は、範囲へ入った最初のフレームではなく、最接近後に行う（ただし厳密には例外あり）

- 行: 1173-1181
- 該当コード:

  ```js
  const d2=Math.hypot(ball.x-prim.cx, ball.y-prim.cy);
  const closing = d2 <= (ball.dPrev===undefined ? 1e9 : ball.dPrev);
  ball.dPrev = d2;
  if(!ball.landed && ball.canCatchAir && ball.t>0.45 && ball.z>1.0 && ball.z<10 && d2<4.5 && (!closing || d2<1.2)){
    const c=resolveCatch();
    return beginThrowPhase(prim, 'fly', 0, c.lead);
  }
  ```

- 何が起きるか: 最初に`d2<4.5`になっただけでは、`dPrev`より距離が短くなっている限り捕らない。その後、距離が縮まらなくなったフレームで捕るため、主要な判定は最接近を意識したものになっている。
- なぜ問題か: 完全な「最接近時」ではない。`d2<1.2`なら`closing`が真でも捕るため、フレーム間隔が大きく、最初の観測が1.2ft以内に入った場合は、まだ近づいている途中でも捕球が確定する。
- 具体的な修正: `d2<1.2`の例外をなくし、前フレームより近くなくなったことを必須条件にする。固定刻み化したうえで、距離が最小になった時点を補間すればさらに正確になる。
- 重大度: low

### 2-4. runnerETAは実際の走者移動と同じ加速モデルではない（確定）

- 行: 694-705, 759-763, 1875-1882
- 該当コード:

  ```js
  if(r.p < r.goal){
    r.v=Math.min(top,(r.v||0)+acc*dt);
    r.p=Math.min(r.goal, r.p + r.v*dt*RUN_SPEED);
  }else if(r.p > r.goal){
    r.v=Math.min(top*0.8,(r.v||0)+acc*dt);
    r.p=Math.max(r.goal, r.p - r.v*dt*RUN_SPEED);
  }
  ```

  ```js
  const d=(b-r.p)/RUN_SPEED, top=r.sp||23, acc=top/ACC_T, v=r.v||0;
  const tAcc=Math.max(0,(top-v)/acc), dAcc=v*tAcc+0.5*acc*tAcc*tAcc;
  return d<=dAcc ? (Math.sqrt(v*v+2*acc*d)-v)/acc : tAcc+(d-dAcc)/top;
  ```

- 何が起きるか: 実際の移動はフレームごとの加算で、帰塁時は最高速を`top*0.8`に制限する。一方、`runnerETA`は常に`top`へ加速する連続式で、帰塁時の0.8倍制限や方向転換を扱わない。`retreatRunners`も目標だけを変え、速度`r.v`を0へ戻していない。
- なぜ問題か: `runnerETA`は送球先の決定と、`judgeAtBase`でランダウンへ送るかどうかの判定に使われている。見た目の走者がまだ加速中、帰塁中、方向転換直後などでは、予測した到着時刻と実際に動く走者の到着時刻がずれる。
- 具体的な修正: 走者を1ステップ動かす関数を共通化し、`runnerETA`はその関数を同じ刻みで仮想実行する。少なくとも帰塁時の最高速、方向転換時の速度リセットまたは減速、現在の`r.cmd`をETA側にも反映する。
- 補足: オーナー実測の走力88で約3.58秒、走力50で約4.32秒という基準は、`A.speed`と`runTime`の式（行455、466-469）から再現でき、この基準自体とは矛盾しない。ただし、その式だけでは帰塁や送球競争の一致までは証明しない。
- 重大度: medium

### 2-5. 最終判定を完全に別の予測値だけで決めてはいない（問題なし。ただし2-1、2-2、2-4の例外あり）

- 行: 842-867, 984-1009
- `updateThrowPhase`は実際に動いた送球が到着した後に`judgeAtBase`を呼び、`judgeAtBase`も実際の`r.p`、`r.goal`、`r.out`を参照している。送球先の候補選びに予測値を使っていても、最終的なアウト・ランダウン・プレー終了の分岐は実オブジェクトの状態を使う構造である。
- ただし、その実オブジェクトの位置自体を、先読みとETAがずれた状態で判定しているため、2-1、2-2、2-4はこの項目の品質条件を部分的に損なう。ここは「最終判定が予測値だけ」という独立バグは確認できない。
## 3. 状態管理

### 3-1. フェーズ切り替えに未処理のまま止まる状態は確認できない（問題なし）

- 行: 560-564, 1388-1404, 1692-1701, 1797-1804
- `update`のswitchには`msg`、`windup`、`pitch`、`flight`、`throwing`、`play`がある。`aimPitch`はswitchにないが、CPU攻撃時に`beginAtBatPhase`で設定され、マウス操作の1695-1697行で`launchPitch`へ進む。CPUの球種ボタンも1797-1804行で動作する。
- 結論: `aimPitch`を毎フレーム更新する必要はなく、入力を待つフェーズとして成立している。今回のコード上、フェーズ名だけが残って進行不能になる経路は見つからなかった。

### 3-2. newGameが再試合用の状態をすべて初期化していない（確定）

- 行: 539-553, 1315-1323, 1849-1853, 1886-1894
- 該当コード:

  ```js
  function newGame(){
    S = { ... over:false, fieldView:false };
    fielders = buildFielders(1);
    ball=null; runners=[]; pitch=null; trail=[]; throwAnim=null;
    ...
  }
  ```

  ```js
  function afterPlay(){
    ball=null; runners=[]; trail=[]; throwAnim=null; throwPlay=null; resetFielders();
    manualThrow=null;
    ...
  }
  ```

- 何が起きるか: `newGame`は`ball`、走者、投球、軌跡、送球演出は初期化するが、`throwPlay`、`manualThrow`、`paused`、`rosterOpen`を初期値へ戻していない。これらは`afterPlay`では消えるが、再試合ボタンは直接`newGame`を呼ぶ。
- なぜ問題か: ポーズ中に「もう1試合」を押すと、新しい試合の状態だけ作られても`paused=true`が残り、時間が進まない。選手能力画面を開いたまま再試合を開始した場合も`rosterOpen=true`が残る。送球先指定の表示や、異常終了から直接再試合した場合の`throwPlay`も持ち越し得る。
- 具体的な修正: `newGame`で少なくとも次を明示的に初期化する。
  ```js
  throwPlay=null; manualThrow=null; paused=false; rosterOpen=false;
  ```
  画面モーダルを閉じる処理も、`rosterOpen`を戻すなら同じ初期化箇所に置く。
- 重大度: medium

### 3-3. 攻守交代時のfielders入れ替えは、現在の半回と整合している（問題なし）

- 行: 1324-1339, 494-500
- `endHalf`は表から裏へ進むときに`S.half=1`としてから`swapFielders`を呼び、裏から表へ戻るときは`S.half=0`としてから呼ぶ。`swapFielders`は`buildFielders(1-S.half)`を使うため、表ではCPU守備チーム1、裏ではプレイヤー守備チーム0になる。
- 捕球・走塁・送球・描画の各処理はグローバルな`fielders`を読む。半回の切替時には`afterPlay`が先に進行中の`ball`、`throwPlay`、`throwAnim`を消しているため、古い守備者配列を新しい半回で使い続ける経路は確認できない。

### 3-4. 「投手へ返球」の指定が、実際には一塁へ送られる（確定）

- 行: 744-745, 819, 1780-1782, 1849-1853
- 該当コード:

  ```js
  function throwPoint(t){
    if(t==='P') return [0,60.5];
    return [BASEPOS[t%4].x, BASEPOS[t%4].z];
  }
  ...
  if(!isPlayerBatting() && manualThrow!=null) tgt = {nb: manualThrow==='P' ? 1 : manualThrow};
  ```

  ```js
  if(throwPlay && throwPlay.stage==='transfer') throwPlay.target = (t==='P') ? 1 : t;
  ```

- 何が起きるか: UIは`P`を「投手」と表示し、`throwPoint`も`P`なら投手位置を返す。しかし手動指定を実際の送球先へ反映する2か所で、`P`を数値`1`へ変換している。
- なぜ問題か: 守備中に0キーまたは「投手」を選ぶと、画面上の指定表示と実際の送球先が一致しない。これは表示と判定の不一致でもある。
- 具体的な修正: `P`を変換せずに保持する。
  ```js
  tgt = {nb: manualThrow};
  throwPlay.target = t;
  ```
- 重大度: medium

### 3-5. 9回制の表示に対して、コードは最大12回まで進む（確定）

- 行: 1741-1744, 1324-1336
- 該当コード:

  ```js
  const maxI=Math.max(9,S.inning);
  ...
  if(S.inning>=12) return gameOver();
  S.half=0; S.inning++;
  ```

- 何が起きるか: 画面は少なくとも9回を表示する設計だが、同点のまま進む場合、`endHalf`は12回裏終了までは`gameOver`にしない。9回終了で同点なら10回、11回、12回へ進む。
- なぜ問題か: 依頼対象は9イニングのゲームなので、同点時の延長を仕様に含めないなら、ゲームの終了条件と表示される試合形式が一致しない。
- 具体的な修正: 9回制を厳守するなら`S.inning>=12`ではなく、9回裏終了後の同点も`gameOver`にする。延長12回を意図した仕様なら、タイトル・説明側に延長を明記する。
- 重大度: low
## 4. 7つの能力値が実際に効いているか

### 4-1. 走力・肩力・捕球・ミート・弾道は実使用が確認できる（問題なし）

- 走力: 行498で`A.speed(p.走)`を野手の`sp`へ入れ、行682、1270、1383でも走者の速度へ入れている。野手の移動は723-731行、走者の移動は694-705行で`sp`を使う。
- 肩力: 行498で`A.arm(p.肩)`を野手の`arm`へ入れ、行747-755の`launchThrow`で送球初速に使う。
- 捕球: 行458-459の`A.transfer`と`A.fumble`を、行804、824-825、926、1001で使う。捕球値が低いほど持ち替え時間と捕球ミス率に影響する。
- ミート: 行1422-1425で`A.meet(bat.ミ)`を打撃タイミング・狙いの許容幅へ掛けている。
- 弾道: 行1425で`A.loft(bat.弾)`を`evalContact`へ渡し、行628の打球角度に使う。
- 結論: この5項目は、変換関数から実際のゲーム計算へ到達する使用箇所がある。今回の静的確認では、同じ能力値が意図せず二重に初速・速度へ掛かっている箇所は確認できない。

### 4-2. 守備力の「守備範囲」部分が使われていない（確定）

- 行: 447-462, 498, 1173-1180, 723-731
- 該当コード:

  ```js
  zone:  v => 0.0175 - 0.00016*v,
  ...
  sp:A.speed(p.走), zone:A.zone(p.守), arm:A.arm(p.肩), fld:p.守, cat:p.捕,
  ```

  ```js
  const d2=Math.hypot(ball.x-prim.cx, ball.y-prim.cy);
  ...
  if(... && d2<4.5 && ...){
    const c=resolveCatch();
  }
  ```

- 何が起きるか: `A.zone`で守備力から範囲値を作り、`f.zone`へ保存しているが、その後の移動・捕球距離は固定条件で、`f.zone`を読む箇所がない。捕球距離は全野手共通の`d2<4.5`である。
- なぜ問題か: 守備力の説明は「守備範囲の広さ＋捕ってから投げるまでの動作の速さ」だが、実際に効くのは`fld`を使う持ち替え時間だけで、守備範囲の差は判定に反映されない。守備力が高い野手ほど広い範囲を守る、という能力値の意味が半分だけ実装されている。
- 具体的な修正: 捕球判定の半径を`4.5 + zone由来の補正`のように`prim.zone`から計算する、または先読み候補の許容範囲へ同じ補正を入れる。表示上の説明を「持ち替え時間だけ」に狭める方法もあるが、7能力値を効かせる目的なら前者が必要。
- 重大度: medium

### 4-3. パワーは実際には1回だけ効き、startFlightのpowerMul引数は死んでいる（確定）

- 行: 617-625, 1089-1093, 1425-1428
- 該当コード:

  ```js
  const c=evalContact(dtMs,dx,dy,A.power(bat.パ), D, A.loft(bat.弾));
  ...
  startFlight(c, A.power(bat.パ), [pitch.tx, Math.max(1.2,pitch.ty), 1.4]);
  ```

  ```js
  function startFlight(c, powerMul, from){
    const v=c.exit*1.467;
    ...
  }
  ```

- 何が起きるか: `A.power`は`evalContact`内で`c.exit`を作るときに使われる。`startFlight`にも同じ値を渡しているが、関数内で`powerMul`を一度も使っていない。
- なぜ問題か: パワーが二重に掛かるバグではないので、実際の初速は一度だけ能力値で変わる。ただし、呼び出し側は二度目の適用を想定しているように読め、将来の修正で誤って二重適用しやすい。引数が不要なまま残っている。
- 具体的な修正: `startFlight(c, from)`へ縮めて、1428行の`A.power(bat.パ)`を削除する。あるいは`powerMul`を本当に使う設計にするが、その場合は`c.exit`へ二重に掛けない。
- 重大度: low

### 4-4. 能力値の適用回数の整理

| 能力値 | 実際の使用 | 判定 |
|---|---|---|
| 弾道 | `doSwing → A.loft → evalContact` | 問題なし |
| ミート | `A.meet → D.tw / D.aw` | 問題なし |
| パワー | `A.power → c.exit` | 1回。`startFlight`の引数は未使用 |
| 走力 | 野手・走者の`sp`と加速 | 問題なし |
| 肩力 | `arm → launchThrow`の初速 | 問題なし |
| 守備力 | `fld → transfer`、`zone`は未使用 | 守備範囲だけ未反映 |
| 捕球 | `cat → transfer / fumble` | 問題なし |

- 結論: 「7能力のうち1つが完全に無効」ではないが、守備力の範囲部分だけが無効である。またパワーは二重適用ではなく、未使用引数が残っている状態である。
## 5. その他

### 5-1. 明らかなメモリリークや無限ループは確認できない（問題なし）

- 行: 646-657, 1308-1311, 1896-1913
- `planPlay`のループは1700回で上限があり、スコア配列の`while`も回数・イニング数に応じて有限回で終わる。`requestAnimationFrame`と`setInterval`はゲームを動かすための常駐ループで、同じループを無制限に追加生成してはいない。
- `ui`はHTMLを作り直すが、古いDOMは画面から外れた後に通常のガベージコレクション対象になる構造で、配列へ無制限に保存している箇所は確認できない。
- 結論: コードから直ちに分かるメモリリーク・無限ループは問題なし。実行中のブラウザメモリ量まではこの静的レビューでは未測定。

### 5-2. 使われていない状態・関数が残っている（確定、動作への直接影響は未確認）

- 行: 773-781, 1091-1095, 1160-1168, 535
- 該当コード:

  ```js
  function leadForceTarget(){
    ...
  }
  ```

  ```js
  ball.pred=plan.firstLand;
  ...
  ball.wallHit=true;
  ```

  ```js
  let cam={eye:[...], at:[...], teye:[...], tat:[...]};
  ```

- 何が起きるか: `leadForceTarget`は定義されているが呼び出し箇所がない。`ball.pred`、`ball.wallHit`、`cam.teye`、`cam.tat`も値を設定するだけで、別の計算・表示で読まれていない。
- なぜ問題か: 直ちに試合結果を壊すとは確認できないが、実際に使われる状態と使われない状態の区別が難しく、後の修正で「設定したから効いている」と誤認しやすい。特に`leadForceTarget`は強制進塁の処理をまとめる名前なので、存在だけを見ると使われているように見える。
- 具体的な修正: 本当に不要なら削除する。将来使う予定ならコメントで未使用と明示し、使うまで結果判定の根拠にしない。
- 重大度: low

### 5-3. 数値について、オーナー実測値と明確に矛盾する箇所は確認できない（問題なし）

- 行: 455, 464-469, 679, 749-755, 636-644
- `A.speed`は走力88を約28.50ft/秒、走力50を約23.10ft/秒へ変換し、`runTime`は90ftを加速込みでそれぞれ約3.58秒、約4.32秒にする。これは提示された走塁基準と一致する。
- `RUN_SPEED=1/90`も、走者の連続値1区間を90ftとして扱うコードと対応している。送球も距離と肩力から初速を計算し、重力・減衰を`stepBall`へ一貫して渡している。
- 結論: オーナーが提示した、総得点9.1、ランダウンのアウト率、捕球距離、三塁ゴロの送球時刻と矛盾することは、コードを読むだけでは確認できない。これらの実測値をこのレビューで再生したわけではないため、実測値そのものの再現性は未検証。

### 5-4. このレビューで実行プレーまでは再現していない（未検証事項）

- ブラウザで9イニングを実際に操作し、全分岐を再生した検証はしていない。したがって、上記の「問題なし」は、該当コードの条件・状態更新から見た判定であり、実プレーで発生するタイミング依存の全てを排除した意味ではない。
- 特に、2-1、2-2、2-4、3-2はコードから直接確認できるため確定した指摘とした。一方、オーナーの測定値と実プレーの再現性は、今回の静的レビューでは未検証として残す。

## 6. まとめ

最優先は、強制判定の前提（1-1）と、同じプレー内で「三つ目のアウト」を特定せず得点を消している処理（1-2）である。次に、表示と判定のずれを生む飛行刻み・壁反射後の担当者・走者ETA（2-1、2-2、2-4）、再試合時の状態残留（3-2）、投手指定の変換誤り（3-4）、守備範囲能力の未使用（4-2）を直すべきである。

| 重大度 | 該当箇所 | 内容 |
|---|---|---|
| high | 1-1 / 行765-771, 984-994 | `isForced`が`origin=0`の打者走者を確認せず、強制進塁の消滅を誤る |
| high | 1-2 / 行1011-1025 | 三つ目のアウトではない`force`または`fly`があるだけで得点を0にする |
| medium | 2-1 / 行646-653, 1117-1123 | 先読みは1/240秒、実際の飛行は`dt/4`で、刻みが一致しない |
| medium | 2-2 / 行1117-1129, 1155-1181 | 壁反射で担当野手を変えた同じフレームに、古い野手と距離履歴を使う |
| medium | 2-4 / 行694-705, 759-763, 1875-1882 | `runnerETA`が帰塁・方向転換を実際の移動と同じに扱わない |
| medium | 3-2 / 行539-553, 1886-1894 | `newGame`が`paused`などを初期化しない |
| medium | 3-4 / 行819, 1849-1853 | 「投手」指定の`P`を一塁の`1`へ変換する |
| medium | 4-2 / 行456, 498, 1173-1180 | `A.zone`で作った守備範囲値が読まれず、守備力の範囲差が効かない |
| low | 2-3 / 行1173-1181 | `d2<1.2`の例外では、最接近前でも捕球する |
| low | 3-5 / 行1324-1336 | 9回制の依頼に対し、同点時は12回まで進む |
| low | 4-3 / 行1089-1093, 1425-1428 | `startFlight`の`powerMul`引数が未使用 |
| low | 5-2 / 行535, 773-781, 1091-1095, 1160-1168 | 未使用の関数・状態が残っている |

フェンス三分岐、ランダウンの判定順、犠牲フライの基本分岐、ダブルプレーの基本経路、満塁の押し出し四球、フェーズ遷移、守備陣の半回交代、明らかなメモリリーク・無限ループ、提示された走塁基準との数式上の整合は、コード上「問題なし」とした。ただし、ダブルプレーは1-1、犠牲フライは1-2の修正後に再確認が必要である。